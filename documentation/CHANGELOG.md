# Changelog

All notable changes to Venture Map are recorded here.

---

## [Unreleased]

### Added
- **The invoice is now the money record — and it no longer needs a booking.** Until now every invoice, payment and earnings figure hung off a booking: `invoices.booking_id` and `payments.booking_id` were both `NOT NULL`, so billing work that had no time slot was impossible, and the Invoices page was literally a filtered list of bookings. An invoice is now a first-class record carrying its **own client, worker, service, title and service date**, so it survives its booking being deleted and can stand completely alone. New `invoice_lines` table (normalised charges with a real `service_id` FK, so per-service reporting groups by *line* — one invoice can mix services), new `invoice_counters` + `next_invoice_seq()` (an `UPDATE … RETURNING` row lock, so two concurrent issues can't collide), and a new **`invoice_list`** view that drives *from* invoices and LEFT JOINs outward — mirroring `work_board`, and the reason a bookingless invoice can finally be displayed. Writes go through a new **`save_invoice`** RPC with patch semantics (only the keys you send are written), replacing a PostgREST upsert that could only ever write line items keyed on a booking. **Migration was additive and provably lossless**: all 58 invoice numbers unchanged, billed €14,590, collected €11,850 and outstanding €2,740 all preserved, verified by a per-row diff of the old and new models that returned zero differences. `line_items` JSONB is still dual-written, so the printable invoice, the public pay page and payment-success are untouched — and **the migration itself needed no Edge Function change**, because a trigger fills in `payments.invoice_id` from the `booking_id` they already write. (Edge Functions *did* change elsewhere in this release — the double-charge clamp, the calendar-sync rollback, and the new invoice pay-by-link path below — so treat this as a statement about the schema migration, not about the release.)
- **A client can pay an invoice by link, with no booking behind it.** The share link now carries a **Pay by card** action, and the money attaches to the *invoice*. New `create-invoice-payment-intent` Edge Function: it resolves the invoice from its own share token and computes the amount **server-side** from that invoice's lines and payments — the page never sends a figure, so a stale tab cannot ask to be charged the wrong one. Deliberately a separate function from `create-payment-intent`, which reads `booking.allow_card`, `deposit_percent` and `price_total` throughout and is live with client pay-links in circulation. `_shared/record-payment.ts` — the single writer of money, shared by the success page and the Stripe webhook — now accepts an intent naming an **invoice** instead of a booking; previously it returned early when `booking_id` was absent, which for an invoice payment would have meant money moving at Stripe and **nothing recorded at all**. When there is no booking it skips the booking-confirm and calendar-sync steps, which have nothing to act on. Standalone invoices are **balance-only** (no deposit), so deposit wording is structurally impossible there rather than conditionally hidden.
- **Invoices now have their own share links, separate from booking pay links.** New **`invoice_links`** table: a token that addresses an *invoice* and grants only "view this invoice" — unlike a `booking_links` pay token, which also unlocks card payment and the delivery paywall. The table is `GRANT SELECT` only; every write goes through `create_invoice_link` / `revoke_invoice_links`. The printable bundle moved into a **`private` schema that PostgREST does not expose** (`private._invoice_bundle(p_invoice, p_booking)`, enterable by either key), with `public._invoice_bundle(uuid)` kept as a thin wrapper — deliberately `CREATE OR REPLACE` rather than `DROP`+`CREATE`, so no caller changed and the function's privileges were never reset.
- **A booking can now carry several invoices.** An issued invoice is a document the client already holds, so when scope grows you raise another rather than editing the one they have — a deposit plus a final, or a supplementary for added work. The booking-detail page gained an **Invoices** card listing them, with **"+ Add another invoice"** (`save_invoice`'s `new_invoice` flag). There is deliberately **no `UNIQUE(booking_id)`**: *"the booking's invoice"* means the **oldest** everywhere — `save_invoice`, the printable bundle and the editor — so an existing share link can never silently jump to a later document, and `set_payment_invoice` routes a booking-keyed payment to the oldest invoice **that still has an outstanding balance**. One invoice still belongs to at most one booking; consolidated billing across several jobs is not built.
- **`confirm-payment` settles invoices, not just bookings.** On return from Stripe it now probes an invoice share token first, and when the intent names an invoice it settles *that invoice* and returns its totals — so the success page shows the right figures for a payment with no booking behind it, instead of falling through the booking-only path and reporting nothing.
- **Create an invoice with no booking.** A **New invoice** button on the Invoices page opens the editor with the job picker set to *"No job — invoice only"*; attach a client (or a one-off name), a worker, a service and the dates, and it takes the next number in the same continuous series. The editor now opens three ways — `invoices/new`, `invoices/edit/:invoiceId` (by invoice id, the only way to reach one that has no booking) and the existing `invoice-edit/:id` (by booking id, so the booking detail page's links keep working). Picking a job adopts its title and date so a linked invoice starts consistent, and actions that only make sense with a booking — Preview, View/print, Copy share link, Reset to booking — are hidden when there isn't one, rather than left as dead links.
- **Hidden Pool, Qala (id 79)** — an unnamed turquoise sea pool inside a limestone grotto on Gozo's eastern tip (Qala), reachable only by water (swim/kayak/boat). Rated the hardest access on the "30 Places" list (`difficulty: hard`). Cover + 3-photo gallery; a coastal walking route (parking near Qala → shore path → the pool spot, ~1.5 km), with the final drop to the water left as a swim/scramble; prerendered + in the sitemap; locality `Gozo-Qala`. Fills the final trend slot **#30**.
- **Client deliveries — hand over the material on the booking link once it's paid.** Attach a **message and/or labelled links** (Drive / WeTransfer / gallery) to a booking from a new **Delivery** card on the booking-detail page, and the client sees them on `/book/:token` **only once the booking is paid in full** — card payments and cash/bank payments you record both count, since they share the same ledger. Until then they see a **locked teaser** ("2 links waiting — unlocks once paid in full, €400 remaining") that carries no content. A **"Release now"** override hands it over early (goodwill, or €0/comped bookings that can never read as "paid"), and **Remove** clears it. New `deliveries` table (1:1 per booking, `links` JSONB, `released_at`) following the `invoices` pattern; new `ui/links-editor/` built on the `line-items-editor` recipe. **The paywall is enforced in SQL** by `get_delivery_by_token` (SECURITY DEFINER) — when locked it returns no message and no links, so the content never reaches the browser. The page renders off the server's `unlocked` flag, never the page state, so a released or €0 booking works too.
- **Tentative bookings + a real confirmation workflow.** The booking form's confirm checkbox is now **"This booking is already confirmed"**, **unchecked by default**. Unchecked → the booking is created **tentative** (`pending`): its slot is **held** for you (admin-created `pending` now blocks the slot; self-serve client cash-requests stay non-blocking), it's **not** on your calendar yet, and it lands in the **"To confirm"** list/dashboard for a one-click **Confirm**. Checked → **confirmed** (`booked`) + pushed to Google Calendar immediately, as before.

### Security
- **Pay-link tokens are no longer enumerable.** `bookings`/`booking_links` were granted `SELECT` to `anon` with token policies that contain **no token predicate** (RLS can't see the client's `.eq('token', …)` filter), so anyone holding the public publishable key could list **every active pay link across all orgs** and read the bookings behind them. The pay page now loads through a new **`get_booking_by_token`** SECURITY DEFINER RPC, where the token is a real predicate and only the 14 fields the page renders are returned (explicit `jsonb_build_object`, so a new column can't silently leak). It also filters both soft-delete flags, so a deleted booking's link correctly reads as invalid. The anon `SELECT` grant and the two token policies (`bk_token_r`, `bl_token`) are now **revoked/dropped**, after verifying the RPC-based build was live: enumerating `booking_links` as anon went from **76 tokens returned** to `42501 permission denied`, while the pay, invoice and delivery RPCs keep working. **`anon` now holds no data privileges on any table** — everything public goes through SECURITY DEFINER RPCs or service-role Edge Functions.

### Fixed
- **The invoice a client opens on their phone scrolled sideways — on the one number they came to read.** The public invoice stylesheet had **no breakpoint at all** except `@media print`, so the A4 sheet kept its full `48px 52px` padding on a 400px screen (26% of the viewport spent on margins), the header never wrapped (`.inv-meta` is `flex: 0 0 auto`, and a supplier email has no break opportunity under the default word-break), and `.totals` was a fixed `280px` block inside a ~264px content box — so `margin-left: auto` collapsed and the **Total / Balance due rows overflowed the sheet entirely**. The sheet now reduces its padding below 760px, the header stacks, and the totals block is `min(280px, 100%)`. Charge descriptions also wrap on any character, since a pasted URL in a line item would otherwise set the table column's minimum width. (`public/invoice/invoice.component.scss`)
- **Every state screen of the client pay link overflowed horizontally.** `.book-state` — which renders *expired*, *confirmed*, *already paid*, *request sent* and the error states — combined `width: 100%` with `padding: 60px 24px` and **no `box-sizing: border-box`**, so the 48px of padding was added on top of the full width. The same defect sat on the shared `.delivery` card; `.paid-page .delivery` already carried its own `box-sizing` for exactly this reason, so it's now set on the base rule rather than patched at a third call site. (`public/book-page/book-page.component.scss`)
- **A charge line couldn't be deleted on a phone, and neither could a work task.** The line-items editor is a `1fr 120px 30px` grid, but a grid track's `1fr` floors at its content minimum and the description cell is a `<textarea>` (~20 columns intrinsic) — so the row stayed wider than the card and the 30px **×** was clipped off the right edge. It now uses `minmax(0, 1fr)` and drops to two rows below 560px. Separately, the work board's task delete was `opacity: 0` revealed only by `:hover`, which never fires on touch, leaving it permanently invisible; it is now always shown under `@media (hover: none)` with a real touch target. (`ui/line-items-editor/`, `platform/work/work-board.component.scss`)
- **The extra-hour rate was unreachable when editing a service.** `.row` gave each field `flex: 1`, which sets flex-basis to 0 but leaves `min-width` at `auto` — so each field still floored at an `<input>`'s intrinsic width (~170px) and the *second* field, the extra-hour rate, was pushed off-screen. Fields can now shrink and stack below 560px. The shared `.item` row had the matching defect: it couldn't wrap, `__actions` is `nowrap`, and **`.item__main` was styled nowhere at all** despite every consumer rendering it — on a phone the Staff row's three buttons left ~92px for the name. Also widened the `.link-btn` touch target, which was ~23px tall. (`platform/services/`, `platform/admin.shared.scss`)
- **Assorted admin rows no longer clip their own content on a narrow screen.** The booking-detail fact rows and the cash-request approve/decline rows now wrap instead of squeezing free text the admin typed; the dashboard chart columns and the new Cash received / Work done control can shrink and wrap within their card; and the invoice editor's two-up field rows use `minmax(0, 1fr)` so a long client or service name in a `<select>` can't force the row wider than the card. Removed a dead `.item` grid from the invoice editor that styled markup the template no longer renders. (`booking-detail`, `booking-list`, `dashboard`, `invoice-edit`)
- **A client could be charged more than they owed.** `create-payment-intent` priced "Pay in full" and "Pay deposit" straight off `bookings.price_total` and consulted the `payments` ledger **only** in the `remainder` branch — so it never checked what had already been paid. The pay page computes its state once on load and never refreshes, so a client holding a stale view (browser Back out of the success page, a second tab, or a cash payment you record while they sit on it) could pay a 30% deposit and then tap **Pay in full** and be charged the whole job again — 130% collected, as two separate PaymentIntents that the idempotency key cannot catch. The prior-payments sum is now computed once for **every** payment type, the "already fully paid" rejection applies to all three, and the charge is clamped with `Math.min(amount, balance)` so a stale page can never ask for more than is outstanding. (Edge Function `create-payment-intent`.)
- **A calendar import that overlapped a real booking was reported as a success and left the booking invisible.** `sync-calendar` checked the error on its `bookings` insert but not on the `booking_slots` insert that follows, then incremented `pulled` regardless. A `23P01` overlap was therefore swallowed, leaving a booking with **no slot at all** — invisible to both the availability picker and the no-overlap constraint, so that time still showed as free. The slot error is now checked, the orphaned parent booking is rolled back, and the overlap is surfaced in `pull_errors` where it belongs. **Backfilled the 31 pre-existing slot-less bookings** (all from one 2026-06-22 import): 28 got their slots, and the 3 that genuinely overlap were left alone and reported rather than force-inserted — all three are past-dated and appear to be the same job imported twice.
- **A client lost access to their own payment once its invoice was detached.** The `pay_client_r` policy matched on `booking_id` alone, and `delete_booking(p_keep_invoice => true)` sets that to NULL — `NULL IN (…)` is NULL, so the row became unreadable and the client's receipt page went blank. The policy now has an invoice-keyed arm alongside the booking one, with qualified names.
- **The printable invoice and the Invoices list disagreed about what had been paid.** `_invoice_bundle` summed payments by `booking_id` while `invoice_list` and `get_earnings` key on `invoice_id`, so a kept (detached) invoice would print as fully unpaid with the whole balance due. The bundle now keys on the invoice when one exists, falling back to the booking only for rows that have no invoice; the two arms are mutually exclusive, so nothing is counted twice. Applied with `CREATE OR REPLACE` rather than `DROP`+`CREATE` deliberately — re-creating it would have reset its privileges and made the one function in the schema with no internal authorization anon-callable.
- **The cross-tenant guard on invoices and deliveries never actually ran.** `inv_admin` and `del_admin` both ended their `WITH CHECK` with `EXISTS (SELECT 1 FROM bookings b WHERE b.id = booking_id AND b.org_id = org_id)` — but inside that subquery **both** names bind to `bookings`, making the test `b.org_id = b.org_id`: always true. The intended protection (an admin of org A can't attach an invoice or a delivery to org B's booking) was silently absent. Both policies now use qualified names (`invoices.booking_id` / `invoices.org_id`), and `inv_admin` additionally allows `booking_id IS NULL` so standalone invoices are insertable.
- **A soft-deleted pay link still served its invoice.** `get_invoice_by_token` is `SECURITY DEFINER`, so it bypasses the restrictive `hide_deleted` policy — and it resolved the token without filtering `booking_links.deleted_at`. A link you had removed kept working. It now filters it, matching `get_booking_by_token` and `get_delivery_by_token`.
- **One payment was recorded as €175 against a €100 job** (BK-2026-090, "Filming at Beefbar"). Corrected to €100 with the original amount preserved in the payment's note. This was the sole reason the Invoices page's "Outstanding" (€2,665, netted across the whole period) disagreed with the true per-invoice total (€2,740); with it fixed, both definitions agree.
- **Removed payments no longer count anywhere — including two money bugs.** Soft-deleted payments are hidden by a RESTRICTIVE RLS policy, but Edge Functions run as `service_role`, which **bypasses it** — and only one of six payment reads filtered `deleted_at`. Consequences, now fixed: **`create-payment-intent` charged the remaining balance SHORT** (a deleted payment still counted as paid), **`cancel-booking` would have sent a refund for a payment that was already removed**, `confirm-payment` inflated the receipt's paid/balance figures, and the Google Calendar "Payment" line plus `sync-calendar`'s push check both counted removed payments. All six reads now filter `status='completed' AND deleted_at IS NULL`. (`create-payment-intent`, `confirm-payment`, `cancel-booking`, `sync-calendar`, `_shared/booking-event.ts` — plus redeploying every function that bundles that shared file.)
- **Delivery links now look tappable.** The client's delivery rendered each link as a plain bordered box, so it read as a label rather than a button. Links now use the accent border/tint, an underline on hover, and an inline **external-link SVG** (not an emoji, so it renders identically across operating systems). (`book-page.component.*`)
- **Booking pages no longer send paying clients to the Malta tourist map.** A leftover from when the booking app was grafted onto the map site: `BookingInvoiceComponent` defaulted to `backLink = '/malta'` / `'← Back to explore'`, so the receipt on a paid `/book/:token` invited the client to "explore" a tourism map, and `payment-success` hardcoded the same link twice. The shared invoice's back link is now **opt-in** (`backLink = null` — callers choose their own destination), and payment-success points at **"← Back to your booking"** (`/book/:token`, or `/book/mine` when signed in) — where their receipt, invoice and delivery actually live. Swept the whole booking module: no `/malta` references remain. (`booking-invoice.component.ts`, `payment-success.component.*`)
- **A deleted payment no longer leaves the pay link claiming "paid in full".** `check-availability` runs as `service_role` (which bypasses the `hide_deleted` RESTRICTIVE policy) and summed `payments` **without filtering `deleted_at`**, so after you removed a mistaken payment the admin list correctly dropped to *partial* while the client's page still reported **paid** and listed the ghost receipt. Payments are now read as an explicit query filtering `status = 'completed'` **and** `deleted_at IS NULL` (mirroring the `booking_slots` query in the same function), and a soft-deleted booking's link now reads as invalid instead of serving a live pay page. This also makes the new delivery paywall re-lock correctly when a payment is removed. (Edge Function `check-availability`)
- **"Back" from the Stripe form no longer forgets payments already made.** Backing out of the card form always returned to a *fresh* full/deposit chooser, so a part-paid booking lost its context and re-offered the full amount (e.g. €400 / €120 deposit on a booking with €225 already paid and only €175 due). The page now remembers the view you left (`ready` / `choose` / `partial`) and returns you there — a part-paid booking lands back on its remaining-balance view. Same for a failed payment intent. (`book-page.component.ts`)

### Changed
- **Deleting a booking now asks what to do with its invoice.** Removing a job from the calendar and writing off the money it earned are different decisions, so the delete prompt gained a second checkbox beside the calendar one — *"Also delete invoice JFMB-2026-045 (€450 · €200 paid)"* — **defaulting to OFF once anything has been paid** against it, since that's a financial record you probably want to keep. Leave it unchecked and the invoice survives as a **standalone invoice**, keeping its number, lines and payments. New `delete_booking(p_booking, p_keep_invoice)` RPC: keeping detaches the invoice **and its payments** before the delete, so the booking's soft-delete cascade can't reach them — without detaching the payments too, a "kept" invoice would silently have read as unpaid with its full balance owing.
- **The dashboard can now show earnings per worker, and two honest readings of the same period.** A **who** filter (shown once there's more than one worker) switches every money figure between the whole organization and a single person, and a **Cash received / Work done** toggle switches the chart between money *arriving* (bucketed by payment date) and work *happening* (bucketed by the job's date) — with gross and net shown side by side and labelled. All of it comes from one new **`get_earnings`** RPC computed in SQL, so the dashboard, the Invoices page and the printable invoice can't drift apart; there were already four divergent payment-status implementations and a fifth client-side money calculation would have repeated that. Because earnings are now invoice-rooted, **invoices with no booking are finally counted** — the old figures summed bookings and could not see them. A per-worker breakdown appears under "Needs your attention" when there's more than one worker.
- **⚠️ The monthly bars will move, and that's the fix.** The old chart was titled "Collected · last 6 months" but bucketed each booking's *payments* by its **booking start date**, so the total was right while individual months were wrong. Cash is now bucketed by the date each payment actually arrived. The "Review →" link under outstanding also now points at Invoices rather than the bookings list, since outstanding is a property of invoices.
- **A job can join the Work board after it was created.** "Needs production" was only ever offered on the booking form, so a shoot you didn't flag up front could never reach the board — the only way in was to delete the booking and make it again. The booking detail page now shows a **Production** row: a link to the board when the job already has a card, and **"+ Add to Work board"** when it doesn't. Adding one seeds the service's task checklist exactly as the form does, and the button disappears once a card exists so you can't create two. (`booking-detail.component.*`, `BookingAdminService.workItemForBooking`)
- **Payment wording now follows the amounts, not a payment-type label.** A receipt said *"Deposit received. The remaining balance of €X is due on the day"* for **any** partial payment — including cash recorded by hand on a booking where no deposit was ever offered — and both the receipt and the shared payment confirmation printed a **"Balance due €0.00"** row on invoices that were settled in full. The copy is now derived from what has actually been paid against what is owed: the balance row appears only when something is genuinely outstanding, "Deposit paid" becomes plain "Paid", and the confirmation headline distinguishes settled-in-full, still-outstanding and nothing-to-pay. The dead `typeLabel` accessor is gone rather than left for someone to reintroduce, and "due on the day" is dropped entirely — an invoice with no booking has no day. (`payment-success.component.*`, `booking-invoice.component.*`, `book-page.component.html`)
- **"Outstanding" is now summed per invoice instead of netted across the period.** The Invoices page computed `total billed − total collected`, so a single overpaid invoice quietly reduced what everyone else appeared to owe. Each invoice's balance is now floored at zero and summed — one client's overpayment can never cancel out another client's debt. The page also now shows **"Billed (gross)"** and **"Net of expenses"** side by side and labelled (equal until expenses are recorded, which should be visible rather than hidden), and gained **Worker** and **Service** columns.
- **Client-facing pages no longer change width as you move through them.** The pay page rendered its own states at three different column widths — 420px (confirmed / request sent), 480px (booking card) and 600px (paid) — so going from the confirmed view to the card chooser visibly jumped the layout. The message/confirm states now use the same 480px column as the booking card. The paid view stays 600px because that is the width of the invoice document it contains. Separately, `booking-checkout` was 460px while every other step of the self-serve flow (service picker, calendar, my bookings) is 560px, so the column narrowed at the last step — now aligned. (`book-page.component.scss`, `booking-checkout.component.scss`)
- **The client's booking pages now line up as one even stack.** Action blocks sized to their own content, so the Download/Print buttons sat centred and narrow, the delivery card was inset by a gutter its neighbours didn't use, and "Pay by card" was a small centred button — the view read as ragged. The invoice buttons now share their row (`flex: 1 0 auto`) in **all four** places they appear (confirmed, request-sent, paid, part-paid); inside the booking card `.invoice-actions` picks up the same 28px gutter its eleven sibling blocks already use; the paid banner spans its column so its buttons match the delivery card and receipt below; and in the confirmed/request views the delivery card sits flush and the primary action is full width. (`book-page.component.scss`)
- **The client's pay link now itemises the payments received, not just one lumped total.** A part-paid booking said "Deposit of €225.00 already paid" even when that was two separate payments (e.g. €75 by card in June + €150 cash in September) — misleading, and the client couldn't see what had actually been received. `check-availability` now returns each completed payment (`amount`, `type`, `method`, `paid_at` — deliberately no Stripe ids or internal notes) and the page lists them **amount · method · date** under the paid total, on both the part-paid and paid-in-full views (one shared `ng-template`). The banner text also drops the hardcoded "Deposit of". (Edge Function `check-availability` + `book-page.component.*`)
- **A card-only booking with nothing paid now opens straight on the payment options.** A confirmed booking always landed on the invoice view ("You're all set! Here's your invoice") with paying demoted to a secondary "Prefer to pay online?" button — wrong when **card is the only way to settle and nothing has been paid**, since paying is the entire point of that link. Those now open directly on the amount chooser (Pay in full / deposit), with **"← Back to invoice"** one tap away. Bookings that also allow pay-later, or that have nothing left to pay, keep the invoice-first view. Related: the part-paid view no longer offers "Pay remaining balance" when card is disabled (that intent is rejected server-side) — it now states the balance will be arranged directly. (`book-page.component.*`)
- **A confirmed booking's "Pay by card" now opens the amount chooser instead of charging the full total.** The button on the confirmed/invoice view read "Pay €100 by card" and jumped straight to a full-amount Stripe form. It now reads **"Pay by card"** and opens a new `choose` state that reuses the existing options card — **Pay in full** / **Pay X% deposit** (deposit hidden automatically for past bookings, which stay full-payment only) — plus a "← Back" to the invoice. Pay-later is hidden there since the booking is already confirmed. (`book-page.component.*`)
- **"30 Places" trend list completed:** #28 Munxar Path (id 8), #29 Dingli Cliffs (id 19), #30 Hidden Pool, Qala (id 79). No locked placeholders remain — all 30 revealed.
- **Munxar Path (id 8) refreshed + merged with Munxar Window.** Real photos (rock-"window" cover + cliffs/bay + a second arch), migrated from the numeric `8.webp` to a `munxar-path/` image folder (cover + 3-photo gallery + thumb), and a full GPS walking route (parking near the beach → coastal path → clifftop viewpoint); the pin now sits at the viewpoint. The near-duplicate **Munxar Window (id 9) was removed** and its slug folded into Munxar Path as `slugAliases: ["munxar-window"]`, so existing `/malta/locations/munxar-window` links still resolve (and canonical-redirect to `/malta/locations/munxar-path`). `munxar-window` stays in `prerender-routes.txt` (alias page) but drops out of the sitemap (canonical only); old `9.webp` images removed.
- **"Pay later (cash/Revolut/bank)" on the client link is always a request you confirm — never an instant booking.** `accept-inperson` no longer auto-confirms pay-later-only bookings; it marks the booking `pending`, stamps "client agreed to pay in person", and surfaces it in your **To confirm** list (Confirm → books it + calendar). The client now also **sees their invoice** on the "Request sent" screen (was a dead-end), and can reopen the link to re-send until you confirm. Paying by card still auto-confirms.
- **A confirmed booking's link now leads with the invoice.** Instead of pushing payment, a confirmed booking shows "Booking confirmed" + the invoice (download/print); if you enabled card and a balance remains, an optional "Pay by card" is still offered. Admin labels renamed **Approve → Confirm**, **"Requests to approve"/"Cash requests" → "To confirm"**.

### Fixed
- **Basemap "API KEY REQUIRED" watermark gone.** CARTO retired free anonymous access to their Voyager raster tiles and began stamping unauthenticated tile requests with an "API KEY REQUIRED" watermark. Switched the basemap to keyless OpenStreetMap tiles (`tile.openstreetmap.org`). (`map-functions.ts`) — to restore the Voyager look, a free CARTO key (`carto.com/basemaps/apikey`) can be appended as `?api_key=…`.
- **Calendar sync no longer re-imports the app's own events as duplicate "External" bookings.** `sync-calendar`'s pull step deduped only against `bookings.google_event_id` — which holds just a booking's *first* block — so every multi-block booking's later-block events (`… (2/3)`, `(3/3)`) got re-imported as external duplicates on each sync. Now it dedupes against **`booking_slots.google_event_id` too**, skips any event carrying our `[BK-####-##]` ref stamp, and **pulls future events only** (from the start of today, not 120 days back). Cleaned up the 12 existing duplicates safely — nulled their `google_event_id`s first so the shared-with-real-booking calendar events (BK-2026-010/013/034/089) were preserved, then soft-deleted them. (Edge Function `sync-calendar` + one-off data fix.)
- **Card payment on the pay link no longer errors with "Cannot read properties of null (reading 'elements')".** When a booking's org charges via the **platform Stripe account** (no separately-connected account → `create-payment-intent` returns `stripeAccount: null`), `selectPayment()` skipped Stripe.js init and used a null `this.stripe`. It now ensures Stripe.js is loaded (idempotent) before creating elements, and rebinds to the connected account only when there is one. (`book-page.component.ts`)
- **No-coupon experiences book straight through (no empty coupon modal).** Tapping Book on an experience whose provider has no discount (e.g. Gozo Kayak Tours → Viator affiliate link) now opens the booking URL directly in a new tab — no login gate, no coupon-reminder interstitial. Providers with a code keep the reminder. Also guarded the shell's coupon-reminder on `provider.discount` so an empty code box can never render again. (`experience-detail.component.ts`, `map-shell.component.html`)
- **Multi-block bookings no longer show "This slot is no longer available" on their pay link.** `check-availability` did one Google Calendar query over the booking's whole `start_at`→`end_at` span and excluded only `bookings.google_event_id` (the *first* block's event). A booking with 2+ time blocks therefore saw its own later blocks' events inside that window, counted them as conflicts, and returned `available: false` → the client's pay page fell through to the "unavailable" state. Now it checks **each block's own range** and excludes **all** of the booking's own events (one per `booking_slots` block), so a booking never conflicts with itself. (Edge Function `check-availability`.)
- **"Around this spot" ranks the right experience first.** `getExperiencesNearLocation` now puts experiences explicitly tagged to the location (`spot.nearLocationIds` includes its id) above ones that merely fall within the proximity radius, breaking ties by distance. So the Sea Cave Kayak Tour (tagged to L-Għar ta' Santa Katarina) leads that cave's panel instead of a nearby-but-unrelated dive.
- **Event/experience clusters now respond to taps reliably, like location clusters.** Tapping a promo (event/experience) cluster sometimes did nothing: the click used `forEachFeatureAtPixel` with zero hit tolerance, so a tap landing on a transparent part of the cluster's photo/count canvas returned no feature. Added `hitTolerance: 8` to the click + hover hit-detection, and unified both cluster kinds through one `zoomToCluster()` expand path (removed the separate `expandPinCluster`), so event/experience clusters behave exactly like the location clusters that already worked. (`map.component.ts`)
- **Non-prerendered routes no longer flash the landing page on load.** The GitHub Pages fallback (`docs/404.html`, serving the whole booking app, pay links, and share pages) was a verbatim copy of `docs/index.html` — the prerendered **landing** page — so reloading e.g. `/bookings/dashboard` painted the full landing for a frame before Angular booted and routed away. A shared `stripAppShell()` (`scripts/og-meta.js`) now swaps the prerendered `<app-root>` DOM for a neutral boot spinner in `make-404.js` and `make-share-pages.js`; `<head>`/scripts/base-href (and the `noindex` fallback) are untouched, so the app still boots in place and the booking app stays out of Google.
- **Event/experience map clusters are now single-level, like locations.** They cluster by proximity only below `CLUSTER_ZOOM` and show individual pins above it; tapping a cluster animates once to ≥ `CLUSTER_ZOOM` and expands straight to its pins — no more nested "cluster inside a cluster" on zoom-in. (`map.component.ts`: `refreshLayer` gates pin clustering by `shouldCluster`; `expandPinCluster` fits-then-bumps to the threshold.)

### Added
- **New hidden-gem location: Tas-Simar Natural Arch (id 78)** — a wild sea-carved rock arch on Gozo's NE coast in the limits of Qala, reached by a ~1 km coastal walk from Daħlet Qorrot bay. Cover + 3-photo gallery; prerendered + in the sitemap; locality `Gozo-Qala`.
- **Stronger email validation on the magic-link sign-in** (to cut Supabase email bounces) — a proper email regex rejects typos/garbage before any send. Optional Cloudflare Turnstile scaffolding remains in `sign-in-form.component.ts` (gated by `TURNSTILE_SITE_KEY`) but is **disabled** (`''`): the live widget errored for the vast majority of users and blocked sign-in, so it was turned off (and CAPTCHA disabled in Supabase → Auth → Attack Protection). Re-enable only with a fix for the failures.
- **New provider + experience: Gozo Kayak Tours (Xlendi).** A guided "Sea Cave Kayak Tour" (`kayak` experience, new `ExperienceType`) that paddles to L-Għar ta' Santa Katarina; surfaced in that cave's panel (+ Carolina Grotto, Xlendi Bay) via `nearLocationIds`. Booking goes to WhatsApp (`wa.me` deep link) — no coupon/website. Provider + experience pages prerendered; pin at Xlendi.
- **New hidden-gem location: L-Għar ta' Santa Katarina (id 77)** — Saint Catherine's Cave, a large Gozo sea cave in the cliffs west of Xlendi, reached only by water via a ~1 km kayak route launched from Xlendi Bay. Cover + 3-photo gallery; slug `l-ghar-ta-santa-katarina`; added to `prerender-routes.txt`, the sitemap, and the 30-places page (#26).
- **New hidden-gem location: Imġiebaħ Beach (id 76)** — remote sandy bay below Selmun (Mellieħa), with a ~700 m parking→beach walking route. Added to `locations.json`, `prerender-routes.txt`, and the sitemap. _Photos pending._
- **Events & Deals pages show every pin (no clustering).** A `clusterPins` flag (default on) is turned off on `/malta/events` and `/malta/deals` so each event / experience renders as its own pin; the explore map still clusters.
- **Tapping a main filter chip frames that layer.** Selecting Gems / Experiences / Events / a gem theme / All now fits the map to exactly those pins (like opening a cluster) via `fitVisiblePins()` — no more, no less. Replaces the old fixed zoom-to-Malta on filter change.

### Changed
- **Nav interstitial fully off providers → experiences.** The countdown modal's top card is now the nearest **experience** deal (was a provider card); `bridge.interstitialExperience` replaces `interstitialProviders`. Removed the now-dead `ProviderCardComponent`, `interstitialProviderSelected$`, and the provider-near-location lookup — experiences are the model everywhere now.

### Changed
- **Map filter bar simplified to one single-select row.** Replaced the old type/difficulty/island mix (and a short-lived two-tier layer version) with a single straightforward row: `🗺️ All · 💎 Gems · 🤿 Experiences · 🎟️ Events · 🪨 Caves · 🏖️ Beaches · 🏛️ Historical · ⛵ Gozo · 🏝️ Comino` — pick one. Content types first (show only that type's pins), then a few gem themes (show gems filtered to that theme). "Deals" dropped (it's the Experiences type now); Easy/Hard dropped from the map bar (still on the `/malta/list` page). Drives `bridge.mapLayers` + `bridge.filters`; the bar reflects bridge state (no local selection to drift). `MapExploreComponent`'s `effect()` maps the active types → `experiencePins`/`eventVenuePins`; the map's `showGems` input hides location pins when Gems isn't shown. All types visible by default.

### Added
- **Location panel split into "My Map" / "Google Maps" tabs** (below the stats bar). **My Map** shows the full in-app content — photos, description, "Around this spot", Explore Together, Closest Spots — with the route drawn on the live map and **no** external-nav buttons. **Google Maps** is the focused directions view: the open-in-Google-Maps buttons **plus** "Around this spot". The shared nearby block is one `ng-template` reused in both tabs; tab resets to My Map per location.
- **Nav interstitial now shows 1 deal (top) + 1 event (bottom)** instead of two provider cards — the countdown modal after "Open Google Maps" surfaces the nearest experience deal and the nearest upcoming party (`bridge.interstitialEvent`). Tapping the event opens its tickets.
- **Location panel "Around this spot" now lists nearby experiences *and* events.** Renamed from "Experiences near this spot" — it shows the **2 closest** experiences plus the **2 closest upcoming events** within range (`getEventsNearLocation()`), each as the shared `ExperienceCardComponent` / `EventCardComponent`. Cards link out to deals / "What's on". Experience cards now render the **cover photo + corner icon badge** (matching the map pins) instead of a flat icon disc.
- **Promo pins cluster by proximity, keeping a cover photo + count pill.** Dense areas (e.g. Paceville/St Julian's) no longer pile up — nearby pins merge into a cluster that still shows a **representative photo** (events: the soonest upcoming event's poster; experiences: a member's image) with a type badge, and the count lives in the pill above ("27 events" / "N experiences"; event count sums all events at the grouped venues). Falls back to an icon disc until the cover loads. Tapping a cluster zooms to fit its members; they re-cluster live as you zoom. (`clusterPromo()` / `buildPromoClusterCanvas()` in `map.component.ts`, `promo-cluster` feature type.)
- **Events list — compact dropdown filters + a day-of-week filter.** Replaced the tall chip rows with three small selects — **When** (period), **Day** (Any day / Mondays … Sundays, great for recurring nights), **Type** — cutting the filter block to ~one row so events lead the view.
- **Event cards say "Today" / "Tomorrow"** instead of the date when applicable (`formatEventDate(..., now)`).
- **Per-view map layers.** The Events page shows only events + experiences (no gems); provider and experience pages show experiences + events (no gems) — via a new `bridge.showGems` signal that promo pages turn off (explore still follows the filter bar).
- **Event map pins now show the event poster (photo circle + 🎟️ badge).** Posters are downloaded local via the new `scripts/download-event-images.js` and compressed by `convert-to-webp.js` — required because GetYourTickets' image CDN sends no CORS header, so a remote image would taint the pin canvas and break map hit-detection. Each venue pin uses its next event's image, with a ticket-disc fallback while it decodes. Past events also drop off automatically (`upcomingEvents()` filters by date everywhere — list + pins).
- **Events ("What's On in Malta") section.** New `/malta/events` page (shell panel, like Deals): browse Malta events with **period** (Today / This weekend / Next 7 / 30 days / All) and **category** (Parties / Pool & Beach / Nightlife / Concerts / Boat) filters, attractive `EventCardComponent` cards (image, date, venue, category badge, **Get Tickets** → affiliate URL), sorted by next upcoming date. Recurring events are one entry with a `dates[]` array. The map shows **one pin per venue** (ticket disc, in the shared cluster layer); tapping a venue pin filters the list to that venue. Data in `src/assets/events.json` (`MaltaEvent` model + `event.utils.ts`); every ticket link carries the affiliate code. `scripts/parse-gyt-events.js` converts a saved GetYourTickets Malta listing (HTML the user supplies) into `events.json` — grouping recurring dates and reading coordinates from the page's map markers. Linked from the footer nav ("What's On"); SEO + prerender + sitemap wired. Gated by `FEATURES.PROMOTIONS`.

### Added
- **Personal landing page at the root (`johnfabiomb.com/`).** A standalone, focused one-page site positioning John Montaño as a Malta **content creator & drone pilot** — for visitors planning a trip and brands wanting Malta exposure. Sections: hero, stats strip, a Work section with his Malta drone reels (Instagram) in an auto-advancing horizontal carousel (arrows + swipe, pauses on hover), a **"Launch Malta Map" CTA band** (ShadowMap-style — the interactive map / 30 places as the centerpiece), three services (Aerial & Drone, Content Creation, Destination Promotion), about, and a contact CTA (email + Instagram). The software-developer angle was intentionally cut — the page itself is the proof. Uses the profile-photo palette (sandstone + azure + gold) and the Valletta photos (`src/assets/landing/`). Routing: exact `/` → `LandingComponent` (outside the map chrome); `/malta` and `/bookings` unchanged. **Prerendered** (static `docs/index.html`) with full SEO baked in: descriptive title/description, a dedicated **`og-john.jpg` share image** (his golden-hour portrait, 1200×630), correct `og:site_name`/Twitter card (were inheriting the map defaults), canonical URL, and **Person JSON-LD**. Scroll-reveal animations are progressive enhancement. Landing photos optimized (~4.8 MB → ~620 KB).
- **Dynamic invoice-footer keys.** The Settings → Invoice footer now supports template keys that each invoice fills in from *its own* booking: `{deposit}`, `{balance}` (rest to pay), `{depositPercent}`, `{total}` (plus `{restOfPayment}`/`{balancePercent}` aliases). Replaces the previously hardcoded "30% / 70%" wording that didn't follow the booking's actual deposit %. A hint under the field documents the keys. `_invoice_bundle` now returns `deposit_percent`; rendering via `renderInvoiceFooter()`. Also de-hardcoded the "Deposit (30%)" label on the receipt card → "Deposit".

### Fixed
- **Editing a booking now updates its Google Calendar event(s); no more stale/duplicate events.** When you changed a booking's time blocks, `update_booking` preserved events for *unchanged* blocks but silently dropped the event id of any *moved/removed* block — so the old event stayed on the calendar (old hours) while a new one was created. Now `update_booking` returns those orphaned `google_event_id`s, and `sync-booking-event` deletes them before re-syncing, so the calendar always matches the booking's current slots + title (renaming updates the event title too). (Live: RPC + Edge Function; frontend passes the orphan ids.)
- **New bookings weren't getting their Google Calendar event.** `booking_slots`, `invoices` and `work_items` were created without the `service_role` table grant the older tables have, so Edge Functions (which run as `service_role`) hit **`permission denied for table booking_slots`**. `ensureBookingEvent`'s query embeds `booking_slots`, so it failed and returned `null` → no event created (silently). The admin app was unaffected because it runs as `authenticated`, which *did* have the grant — which is why it went unnoticed. This also degraded `cancel-booking`, `purge-booking-events` and payment-confirmation calendar updates (all read slots as service_role). Fixed by granting `SELECT/INSERT/UPDATE/DELETE` on the three tables to `service_role` (live + in `bookings-schema.sql`, where `work_items`/`invoices` are created after the blanket grant). Backfilled the events for the affected bookings. The Google OAuth token was verified healthy.
- **"duplicate key … bookings_org_id_booking_ref_key" when creating a booking.** The `BK-YYYY-NNN` ref generator (`set_booking_ref`) ran with invoker rights, so the restrictive `hide_deleted` RLS policy hid soft-deleted bookings from its `MAX(…)+1` scan — it reused a number already taken by a hidden (soft-deleted) booking, tripping the `UNIQUE(org_id, booking_ref)` constraint. Made the trigger `SECURITY DEFINER` so it counts across *all* bookings (refs are permanent and never reused).

### Changed
- **Booking platform made mobile-friendly.** The admin sidebar is now a proper **collapsible drawer** on mobile (top bar + hamburger + slide-in panel + backdrop, closes on navigation) instead of the cramped horizontal icon strip — so every module is reachable on a phone. Shared `admin.shared.scss` `.page` widened (720 → 1200px) with responsive padding + a wrapping header, so pages use the available width like the Bookings page instead of varying narrow caps. **Booking-detail** rebuilt from a lonely 820px column into a full-width **2-column layout** (billing/facts │ payments/record) that stacks on mobile, with wrapping header actions. Verified every module is responsive: list tables scroll (`.table-wrap`), div-tables (Invoices, Organizations) collapse to 2-col, grids use `auto-fit minmax()`, the Work board is a horizontal-scroll kanban, and forms flow via shared `.field`. The shared primitives (`.page`/`.card`/`.field`/`.list`/`.item`/`.btn`/`.table-wrap`) are the canonical structure to reuse. Fixed remaining horizontal-overflow on mobile (Dashboard + Booking-detail header button groups now wrap full-width; `.main` clips stray sideways scroll as a guard). **New reusable `table-cards` SCSS mixin** (`platform/_table-cards.scss`): any `<table class="table">` with `data-label` cells collapses into stacked, labelled cards below 760px (like the Invoices list). Applied to Bookings, Clients **and Invoices** (the Invoices list was converted from a bespoke div-grid to a real `<table>` so it uses the same mixin and looks identical). `@include tc.table-cards` + `data-label` cells is the one-liner for future tables. The mobile card pins the column label left and groups the value(s) right (no more spread/centered multi-part cells). The page-header system (`.page__head` + `.head-actions`, responsive) is now defined once in `admin.shared` and reused everywhere — local duplicates removed from dashboard, booking-list, client-list, organizations, invoices, invoice-edit.
- **Bookings tabs reorganised + sticky.** Reordered into logical groups with visual separators — *Upcoming · Past* (when) │ *Pending · Unpaid · Paid* (status/money) │ *Cancelled · All* — and the bar now horizontally scrolls instead of wrapping. (The standalone *External* tab was removed — imported bookings still show under Upcoming/Past/All.) The active tab is remembered (localStorage + restored into the URL), so editing a booking or taking an action returns you to the tab you were on instead of snapping back to "Upcoming".
- **Deleting (or cancelling) a booking now removes its Google Calendar event automatically, server-side.** Calendar removal used to depend on the *frontend* invoking `cancel-booking` — so a stale build, an unticked checkbox, or a bulk/DB change left the event orphaned on the calendar (the exact symptom: booking gone from the app, event still on Google). Now a DB trigger (`bookings_purge_calendar`) fires on the `deleted_at`/`status→cancelled` transition and makes an async `pg_net` call to a new **`purge-booking-events`** Edge Function (shared-secret auth via Vault), which deletes the event(s) — booking row **and** every slot — and clears the ids. It's idempotent with the frontend's own removal, and works **regardless of which client did the delete**. Verified end-to-end: soft-deleting via the RPC auto-cleared the linked event. (Also fixed: `purge-booking-events` originally used a `slots:booking_slots(...)` PostgREST embed that silently returned no rows under the service role — split into two queries.)
- **Soft delete everywhere — DB-enforced, nothing is ever destroyed.** Deletes used to be hard `DELETE`s that, for a booking, **cascaded away its payments and invoices** — a real audit/financial risk. Now every item table (`bookings`, `payments`, `invoices`, `clients`, `services`, `staff`, `staff_services`, `tasks`, `work_items`, `booking_slots`, `booking_links`) has a `deleted_at` column, and deletes set it instead of removing the row. Reads exclude soft-deleted rows **at the database level**: each table has a **`RESTRICTIVE` RLS policy** (`deleted_at IS NULL`) that AND-s with every existing read policy, so *all* paths (admin, staff, client, token, anon) hide deleted rows automatically — no query changes needed. The `SECURITY DEFINER` paths that bypass RLS (the `booking_summary`/`work_board` views, `get_busy_ranges`, `_invoice_bundle`) got explicit `deleted_at IS NULL` filters. A trigger **cascade-soft-deletes** a booking's children (and a second trigger cascades a board card → its checklist tasks), and the no-overlap constraint ignores deleted slots so the slot is **freed**. Soft-delete **writes go through a `soft_delete(p_table, p_id)` `SECURITY DEFINER` RPC** — a direct `UPDATE … deleted_at` is rejected by the restrictive policy (Postgres re-checks the now-invisible new row → `42501`), so the RPC performs the write with RLS bypassed while still authorizing the caller against the row's org (`is_org_admin`, allowlisted tables). (`unassignService` stays a hard delete — re-creatable link; `resetInvoice` clears the override in place to avoid the `UNIQUE(booking_id)` collision.) Applied live + verified: restrictive policies, both cascades, and the RPC (admin succeeds, non-admin refused). **Delete is now offered for every booking** (not just external) with an opt-in "remove from Google Calendar" checkbox, and the delete toast reports the *real* calendar-removal result instead of always claiming success.
- **`supabase/bookings-schema.sql` cleaned into a canonical fresh-install script.** Folded the incremental `ALTER ADD COLUMN` migrations (§13–§17: `production_status`/`needs_production`, `task_template`, `features`, Stripe Connect columns, `deposit_*`, `invoice_details`) inline onto their `CREATE TABLE`s; completed the RESET block (now drops `booking_slots`/`tasks`/`work_items`/`invoices` too, removed legacy table drops); removed dead `DROP`s and superseded/historical comments; renumbered sections. No behaviour change — it reproduces the current live structure from scratch. (Not run against live: it begins by dropping all booking tables.)

### Added
- **Invoice: real "Download PDF" + "Print" buttons.** The printable invoice page now has a **⬇ Download PDF** (an actual file download, rendered exactly as shown — A4, no margins/browser chrome, via lazy-loaded `html2pdf.js`; `core/utils/pdf.util.ts`) alongside **🖨 Print** (the existing print dialog). The page accepts `?auto=download|print` so other surfaces can trigger either: the public pay page's confirmed/paid states and the admin "booking created" screen now offer both. (Covers admin `/book/invoice/:id` and public `/book/invoice?token=…`.)

### Fixed
- **Payments: succeeded card payments are now reliably recorded; no more "pending" clutter.** Two problems: `create-payment-intent` wrote a `payments` row **every time the customer picked an amount** (deposit/full), filling the ledger with abandoned-intent "pending" rows; and a **succeeded** payment was only marked paid by the Stripe **webhook**, which for **Connect direct charges** only fires if the endpoint listens to *connected-account* events — so real payments (e.g. a €75 deposit) showed `total_paid = 0` and the receipt showed the full balance unpaid. Now: (1) `create-payment-intent` writes **no** row — an intent isn't a payment; (2) the `/pay/success` page calls a new **`confirm-payment`** function that retrieves the intent from Stripe on return, verifies it succeeded + belongs to the booking, and **records it idempotently** (the reliable path, independent of webhook config) + returns correct totals for the receipt; (3) the `stripe-webhook` now **upserts** a completed payment via the shared `_shared/record-payment.ts` (async backup). The ledger holds only money that actually moved, keyed on the unique PaymentIntent id. (Existing affected booking BK-2026-041 was corrected by hand: the real €75 deposit recorded, 3 abandoned pendings removed → €75 paid / €175 due.)
- **User line breaks (charges/description/notes) are preserved when displayed.** Multi-line text typed in a textarea was rendering as one run-on line on the client pay page and booking detail, because HTML collapses whitespace. Added a reusable global utility `.text-multiline { white-space: pre-line }` (in `styles.scss`) and applied it to the pay-page Details and the booking-detail line items (the invoice already did this). Use `.text-multiline` on any element rendering user free-text.
- **Public pay page kept asking to "Confirm booking" for already-confirmed pay-later bookings.** It decided "already confirmed" only from `google_event_id`, so an admin-created `booked` booking with the calendar push off/failed fell through to the confirm action. It now uses the booking **status** (`booked`/`in_progress`/`done`, with `google_event_id` as a secondary signal) — **but only shows the confirmed state when there's nothing to pay online** (pay-later/no card). A booking that accepts **card** stays payable (deposit/balance) even once confirmed, so it still shows the pay options. `book_page` selects `status`.
- **Booking deep links no longer flash the map before loading.** Opening `/bookings` or a `/book/…` link used to bounce through `/` (the map app) via the old static `src/404.html` redirect (`location.replace('/?redirect=…')`), so the map rendered for a moment before the router jumped to the real page. `make-404.js` already generates a `404.html` that boots the SPA **in place** at the requested URL (no bounce) — the conflicting `src/404.html` (and its `angular.json` asset glob) is now removed so the bounce can't come back. `make-404.js` is the single source of `docs/404.html`. (The map app is never loaded on a booking route, so the old "hide the OAuth token from the map's Supabase client" concern is moot — the token stays on `/bookings`.) Takes effect on the next `docs/` deploy.
- **DB hardening (jm-bookings) — removed stale duplicate functions, validated the name constraint, added FK indexes.** A schema audit found old function overloads left behind by signature changes (`CREATE OR REPLACE` doesn't drop them), two of them `SECURITY DEFINER` with a mutable `search_path` — including an `anon`-callable `get_busy_ranges(start,end)`. Dropped the stale `get_busy_ranges/2`, `current_client_id/0`, two old `create_booking_request` overloads, `upsert_my_client/5`, and the orphaned `seed_production_tasks()` (its trigger was already removed). Validated `booking_has_name` (data was clean). Added covering indexes for previously unindexed foreign keys: `bookings(client_id)`, `bookings(service_id)`, `booking_links(booking_id)`, `staff_services(service_id)`. Applied live + mirrored into `bookings-schema.sql`.

### Changed
- **Bookings — all confirmations now use the in-app modal (no more browser `confirm()`).** New `ConfirmService` + a single `<app-confirm-host>` (mounted in the platform shell, mirroring the toast host) renders confirmations through the shared `<app-modal>`, so they match the booking-delete dialog's style (title, message, Cancel + a red destructive action). Replaced every native `confirm()` across the platform: Work-board card delete, invoice editor leave/reset, payment removal, booking decline + cancel/refund, and worker/service/member removal. `await confirm.ask({ title, message, confirmLabel, danger })`.
- **Bookings list — tabs are URL-driven and fetch fresh server-side data (no local filtering).** The active tab now lives in the URL as `?tab=…` (shareable, survives reload, browser back/forward works), and each tab runs its **own filtered+sorted query against `booking_summary`** on demand (`BookingDataService.queryBookings(tab, search)`) instead of filtering one cached list in the component. Search is server-side too (debounced `ilike` on ref/client/job). Tab badge counts come from per-tab `HEAD` count queries (`bookingTabCounts()`); rows + counts re-pull after approve/decline/cancel/delete. A spinner shows while a tab loads. (Top KPI stats + the NEXT badge still derive from the org-wide list the service maintains.)
- **Bookings list — Job is now the primary column.** Reordered to Ref · **Job** · Client · When (Job before Client) and made the job title the prominent cell (bigger, bold), with the client shown as secondary (smaller, muted).
- **Bookings list — dropped the redundant "Revenue" column.** Revenue (`price_total − price_expenses`) equals Total until expense tracking exists, so the two columns were always identical; removed the column from the table (the "expected revenue" KPI stat is unchanged, and `price_revenue` stays in the data model for the future).
- **Invoice editor — "back" now returns where you came from, and won't silently discard edits.** Opening the editor from the **Invoices** list used to dump you on the *booking* on the way back (confusing); back/Cancel/save now default to the **Invoices** list, and only return to a booking when opened from one (that link passes `?from=booking`). Back/Cancel/tab-close also warn if there are unsaved changes (snapshot-based dirty check), matching the no-accidental-loss behaviour of the modals.

### Added
- **Work board — Backlog, auto-activation, assignees + due dates.** Cards for **future** shoots no longer clutter the active board: a card stays in a new **Backlog** column until its booking has ended, then **auto-surfaces** on the board (`work_items.activated_at` + a `work_board` view computing `is_active` from `now()` — no cron), or you drag/▸ it onto the board early. Standalone to-dos go straight to the board. Each card can be **assigned to a staff member** (`assignee_id` → `staff`, avatar chip + picker) and given a **deliver-by date** (`due_at`, overdue indicator + an "N overdue" header count). The board reads the `work_board` view (resolves client/service/assignee names + `is_active` server-side) and is horizontally scrollable. `create_work_item` sets `activated_at` (standalone → now, booking-linked → backlog). Schema designed to extend (labels, multi-assignee, comments) without rework.
- **Public share links (pay link, invoice, booking) now show a proper social preview (WhatsApp/Messenger/IG/etc.).** They used to preview as a bare "johnfabiomb.com" with no image (or the map's "Explore Malta" card), because social crawlers don't run JS and the SPA fallback served the map's tags. Now: branded Open Graph/Twitter cards baked into the static HTML. The **pay link** (`/book/:token` — token in the path) gets its card from `docs/404.html`, which `make-404.js` now stamps with a "Booking · John F. Montaño" card; the **invoice** (`/book/invoice?token=…`) and **booking start** (`/book`) get dedicated static pages via `make-share-pages.js`. Shared meta rewriting in `scripts/og-meta.js`; two branded 1200×630 images (`og-invoice.png`, `og-booking.png`) generated by `scripts/gen-og-images.js`. GitHub Pages serves each file for its path (query string ignored), so the crawler reads the card while the human still boots the app. **The whole booking app is kept out of Google** (no SEO wanted yet): every booking share page is `noindex` (incl. `/book`), and `/pay/success` was removed from `prerender-routes.txt` so it's served by the `noindex` 404 fallback instead of an indexable prerendered file (`/pay` stays — it's a map page). `noindex` blocks indexing but not social crawlers, so previews still work. Cards are branded (same per type), not per-record. Add a literal-path route to `PAGES` in `make-share-pages.js` to cover more.
- **Work board — standalone cards + delete (Trello-style).** The board used to be a fixed mirror of bookings flagged `needs_production`, with no way to remove a card or add your own. It's now its own list backed by a `work_items` table: each card optionally links to a booking (carrying its client/service/date and seeding the service's task checklist) **or stands alone** (a personal to-do with just a title). **"+ New card"** creates either kind (pick a job from the dropdown, or leave it as a personal card); a **×** on each card removes it — for a booking-linked card that just takes it off the board (booking, invoice and calendar event are kept; it unflags `needs_production`), for a standalone card it deletes it. Tasks now hang off `work_item_id` (so personal cards can have checklists too); stage moves on a booking-linked card still mirror to the booking's calendar "Progress". Existing production cards were migrated 1:1. Marking a *new* booking "needs post-production" still drops a linked card on the board automatically; the board is otherwise managed by hand.
- **Bookings — Google Calendar imports now create a blocking slot + show a customer.** `sync-calendar` was inserting imported events straight into `bookings` with no `booking_slots` row, so under the multi-slot model a future import wouldn't block the worker's availability; it now also inserts a slot (same Google event id) and sets `contact_name` from the event title so the imported booking shows a customer. (Existing imports were unaffected — the migration backfilled their slots; their `contact_name` was set by a one-off data migration.)
- **Bookings — "quick name" customers (one-off bookings without a client record).** The booking form's Customer field now toggles between **Existing client** (the reusable CRM record with company/VAT — unchanged) and **Just a name** (type a one-off name, e.g. "John from Sliema"). The quick name is stored on the booking itself (`bookings.contact_name`), **never** added to the Clients table — so the CRM stays clean and there are no throwaway/orphan client rows to prune. `client_id` was already nullable; a `booking_has_name` CHECK now guarantees every booking identifies its customer (a client, a contact name, or an external import). `booking_summary.client_name` = `COALESCE(client.name, contact_name)`, so quick bookings render their name everywhere (list/detail/dashboard) with no other changes. Invoices still work: a quick booking bills to the bare name with no company/VAT (the `_invoice_bundle` RPC falls back to `contact_name`); use a real client when a VAT-ready invoice is needed. `create_booking`/`update_booking` carry `contact_name`. Fully additive.
- **Bookings — a booking can hold several time blocks (multi-slot).** One booking can now span multiple, non-contiguous time blocks across one or more days (e.g. 13:00–14:00 **and** 16:00–18:00, plus another day) instead of a single start→end span. New `booking_slots` table (org/staff/start/end per block) is the source of truth for availability and double-booking: the worker-partitioned `EXCLUDE` no-overlap constraint moved from `bookings` to `booking_slots` (`booking_slots_no_overlap`), a `slot_set_blocking()` trigger derives each slot's blocking flag from its booking status, and `get_busy_ranges` + the admin `getWorkerBusy` read slots. The booking row keeps an **envelope** (`min(start)…max(end)`) for back-compat. Atomic `create_booking`/`update_booking` RPCs insert the booking + its slots together (a losing concurrent block fails with `23P01` → "slot just taken"); `update_booking` preserves each unchanged block's Google Calendar event id (matched by exact start/end) so edits don't churn/orphan events. The admin time picker was rebuilt to manage a **list of blocks**: tap start + end to add a block, switch days and add more (added blocks persist + highlight on their own day, and are removable from a list above the calendar). Each block becomes its **own Google Calendar event** (`ensureBookingEvent` → one event per slot, titled `… (1/3)`); cancelling/deleting removes every block's event. Booking detail lists all blocks; the list shows a "+N blocks" badge (`booking_summary.slot_count`). Fully additive — existing bookings were backfilled 1 slot each.
- **Experience-centric promo — the map now promotes experiences, not providers.** Introduced an `Experience` entity (`provider → experiences[] → spots[]`): one provider offers many experiences, each appearing as its own map pin(s) at evocative spots. Tapping a pin opens `/malta/experiences/:id` (hero, gallery, the deal, and an "Offered by …" link back to the provider page). Deals resolve per experience and fall back to the provider's. The map shows experience pins (icon-only by default; photo + type badge once an experience gets a `coverImage`) **in the cluster layer**, interleaving with location pins by latitude rather than always on top. The Local Deals panel, the Browse Locations list (interleaved cards + Deals filter), and the location panel's "experiences near this spot" now list experiences via a shared `ExperienceCardComponent`; the map shows experience pins (never provider pins) on every promo surface — provider pins remain only for the Groups feature; the coupon box was extracted into a shared `DealBoxComponent` reused by provider **and** experience detail. Seeded 12 experiences across Santa Maria, Yippee and Block with draft pin coordinates (to be fine-tuned). All gated by `FEATURES.PROMOTIONS`. See `documentation/EXPERIENCES.md`. Provider/brand pages and the nav-interstitial ads are unchanged.
- **Yippee experiences enriched from official brochures.** Rewrote all six Yippee experiences with first-party detail (durations, prices incl. child/infant rates, 21+ self-drive rules, all-inclusive list, full itineraries, seasonality) and added the brochure flyers as gallery images. Renamed the placeholder `yippee-sunset-cruise` → `yippee-round-gozo-cruise` to match the real catalogue (half-day Blue Lagoon cruise + full-day Round Gozo cruise; land sunset variants folded into each tour's description).
- **New location — Il-Kalanka (id 75).** Hidden swimming bay on the Delimara peninsula (Marsaxlokk), south-east Malta, near St Peter's Pool. Added to `locations.json` with three photos, prerender route, and sitemap entry. Distinct from the existing Ta' Kalanka Sea Cave (id 49).

### Changed
- **Bookings — payment-success page rebuilt to the invoice standard.** The post-payment page now shows a proper receipt matching the formal invoice (supplier/company name, the **JFMB** invoice number, line items, Total / Paid / Balance, PAID stamp) and a **Download Invoice (PDF)** that opens the full printable invoice — instead of the old hardcoded receipt that showed the raw `BK-` ref and no company details.
- **Bookings — invoices viewable without sign-in via the pay link.** New `get_invoice_by_token(token)` SECURITY DEFINER RPC (anon-safe; validated by the active booking link) + the printable invoice now accepts `/book/invoice?token=…`. This lets a customer who paid through a token link (and isn't signed in) see and download their invoice — previously `get_invoice` was auth-only. The invoice assembly was refactored into an internal `_invoice_bundle()` shared by `get_invoice` (admin/own-client) and `get_invoice_by_token` (token).

### Changed
- **Bookings — "Sync Calendar" now imports past events too.** The pull window was hard-coded to "now → +90 days"; it now also looks **back ~120 days**, so previous-month jobs import instead of only upcoming ones. `sync-calendar` redeployed.

### Fixed
- **Auth — production login left OAuth/magic-link tokens stuck in the URL (couldn't sign in).** On GitHub Pages, `/bookings` has no prerendered file, so it's served by the `404.html` SPA fallback — which only preserved `pathname + search` and **dropped the URL `#fragment`** where Supabase returns the session (`#access_token=…`). The booking app never received the tokens. `404.html` now carries the hash too (URL-encoded into `?redirect=`, so it's also hidden from the map app's Supabase client on `/`, which would otherwise consume a bookings-project token); `map-root` then navigates to the decoded target with the fragment intact, where the correct client reads it. Requires redeploying `docs/`.

### Fixed
- **Auth — login was impossible (OAuth/magic-link dumped tokens on a dead URL).** Root cause: the jm-bookings Supabase project still had the **dev default Site URL (`http://localhost:3000`) and an empty redirect allow-list**, so Supabase ignored the app's `redirectTo` (`…/bookings`) and bounced every login to `http://localhost:3000/#access_token=…` — even from production. Fixed the project's auth config: Site URL → `https://johnfabiomb.com`, allow-list → `https://johnfabiomb.com/**`, `http://localhost:4200/**`, `http://localhost:3000/**`. **Hardening:** `404.html` (the GitHub Pages SPA fallback for non-prerendered deep links like `/bookings`) now also preserves the URL **`#fragment`** — URL-encoded into `?redirect=` so it isn't dropped and isn't visible as a real `#access_token` on `/` (where the map app's Supabase client would otherwise consume a bookings-project token); `map-root` then routes to the decoded target with the fragment intact. Requires redeploying `docs/`.

### Fixed
- **Bookings — calendar time picker now behaves like a normal range selector** (both the public booking calendar and the admin form). Tap a start, tap an end; once a range is complete the **next tap starts a fresh selection** (instead of extending the old one), tapping the start again gives a 1-hour slot, and a **Clear** link removes the selection. Shared logic in `core/utils/range-select.util.ts`; a half-made selection (start chosen, awaiting end) now shows the start cell highlighted.

### Fixed
- **Bookings — "Sync Calendar" was failing (non-2xx).** Two bugs: **(1) Auth** — the function gated on a **hardcoded `johnfabiomb@gmail.com`**, but the actual org owner signs in as `creator@johnfabiomb.com`, so it 403'd the real owner. Now authorizes via **org membership** (owner/admin of `CALENDAR_ORG_ID`), removing the hardcoded identity. **(2) Import** — the pull step inserted external events into `bookings` **without `org_id`/`staff_id`** (both `NOT NULL` since the multi-tenant migration; the `set_booking_ref` trigger also needs `org_id`), which would have 500'd once auth passed. Now scoped to the calendar's org and attributes imports to that org's owner-worker (falls back to its first bookable staff); the push query is org-scoped too, and each import is wrapped per-event (collected as `pull_errors`) so one bad/overlapping event no longer aborts the sync. `sync-calendar` redeployed.

### Added
- **Bookings — guided charge entry, no separate Hours field, and reusable client creation.** Adding a charge is now a guided step: pick **one of your services** (then **how many hours** → description + price pre-filled, editable) **or** a **Custom charge**. The standalone **Hours field is gone**: charges (pricing) and the calendar **span are decoupled** — per-line hours only feed the invoice description/price, while the booking's start/finish is set by **tapping a start and end on the calendar** (the picker is now Calendly-style range selection; editing prefills the existing span, and not touching it preserves the original duration). Creating a **new client** from the booking form now opens the **same full client editor** (name + email/phone/company/**VAT**/billing address/notes) as the Clients page, then refreshes the list and **auto-selects** the new client — no more half-empty client stubs. The line-items + client editors are extracted into reusable components (`booking/ui/line-items-editor/`, `booking/ui/client-editor/`) used across the booking form, invoice editor and Clients page. `needs_production` flag added (a booking joins the Work board only when ticked; external calendar events never do). Removed the now-dead service dropdown, inline new-client fields and segmented toggle.
- **Bookings — build the invoice up front; invoice line items are the source of truth.** Creating/editing a booking now uses a **multi-line charges editor** (each line = description + price; the **total is their sum**) instead of a single description + price field. Services are no longer a mandatory dropdown — they're an optional **"add a line from a service"** helper that pre-fills a line's description + price (for the chosen hours) and stays editable; the worker is any bookable staff. The breakdown is saved to the `invoices` table on create/edit, so the invoice is **ready immediately**. Conversely, **editing an invoice now updates the booking's total** (and its calendar event) — the line items are the single source of truth (previously invoice edits were decoupled from the booking). The booking **detail page shows a "What's billed" breakdown** + Edit-invoice link. Built on a reusable **`LineItemsEditorComponent`** (`booking/ui/line-items-editor/`) shared by the booking form *and* the invoice editor (zero duplication), and reuses the existing `invoices.line_items` column — **no new tables**. The client/company/VAT still come through the `client_id` relation, never duplicated.
- **Clients — add & edit (incl. billing details for invoices).** The Clients screen was read-only; it now has a **New client** button and every row opens an **edit modal** (name + optional email, phone, company, **VAT number**, **billing address**, internal notes). Saved via `BookingDataService.saveClient()` (insert or update; org-admin RLS already allowed it). Billing fields are optional but, once filled, flow straight onto that client's invoices — which reference the client by **`client_id` relation** (the `invoices` table stores only line items / notes / issue date; client + company details are never duplicated there). The list gained a **VAT** column.
- **Bookings & Invoices — professional tabbed organization + next-first ordering.** The **Bookings** list now opens on an **Upcoming** tab with your **next booking pinned at the top** (and a "NEXT" highlight) — previously it showed the furthest-out booking first. Tabs (each with a live count): **Upcoming · Pending · Unpaid · Paid · Past · External · Cancelled · All**, plus a **search** box (ref / client / job). Imported Google events get their own **External** tab and are kept out of Upcoming/Past so the schedule shows only real jobs. Every tab is **strictly chronological** (forward-looking tabs soonest→latest; history most-recent→oldest — no mixed ordering). **Upcoming** includes everything from **today onward — real and imported (external) events** — so today's and synced calendar items show alongside jobs. The date column shows a friendly relative hint ("Tomorrow", "in 3 days"), and cash requests live under the **Pending** tab (count badge in red). **Invoices** gained matching payment-status tabs — **All · Unpaid · Partially paid · Paid** (with counts) on top of the existing year filter — a per-row status badge, and newest-first ordering; the summary totals reflect the whole selected period.
- **Bookings — imported Google events can be enriched into real bookings, and admin edits sync back to Google.** Imported (external) calendar events now have an **"Edit / enrich"** action in the bookings list; opening one in the form lets you add a client, price, description, change the title/time/location, and Save — at which point it **becomes a normal booking** (`is_external` cleared → the "External" badge becomes Unpaid, counts in revenue, invoiceable) and — when card payment is enabled — **saving now hands you a payment link to send** (the edit flow previously returned to the list with no link). Crucially, **saving any booking edit now pushes to Google Calendar** (previously only payment/amount changes did): the event's **title, time, location and description** are all updated (before, `ensureBookingEvent` patched the description only, and the edit path didn't sync at all). The calendar description now leads with your **free-text Description**, followed by the auto summary (Client / Total / Payment / Ref). `updateCalendarEvent` extended to patch start/end; `sync-booking-event` + the other event-writing functions redeployed. **Note:** this is one-way (app → Google) — edits made directly in Google Calendar don't flow back (the "Sync" pull only imports brand-new events).

### Changed
- **Bookings — admin "New booking" time picker is now worker-based, with a calendar on the right.** Availability depends on the **worker's** calendar (they can't be in two places at once), **not** the service — so the picker now appears for **any** service incl. **Custom**, as soon as a worker is chosen, and shows a full **24-hour** grid. Busy slots reveal **who/what** occupies them (client · title), like Google Calendar — **admin-only** (the public page never receives these details). It reuses a new shared presentational **`AvailabilityCalendarComponent`** (extracted from the public booking calendar — zero duplication) and renders in a sticky right-hand panel beside the form. Replaces the previous service-scoped slot list and the Custom-only manual date-time input; the DB no-overlap constraint still blocks clashes on save. Busy ranges come from an admin-only, org-scoped query (`BookingDataService.getWorkerBusy`); wall-clock↔UTC handled by `core/utils/timezone.util.ts`.

### Removed
- **Bookings — dead code cleanup** — deleted the unused legacy `create-booking` Edge Function (undeployed too; had a hardcoded owner email) and the orphaned, unrouted `my-calendar/` (incl. `working-hours-editor/`), `slot-picker/` components and the now-unused `WorkingHoursService` (legacy `admin_settings` global config; per-worker hours live on `staff_services.working_hours`). CLAUDE.md Key Files updated.

### Changed (multi-tenant IA)
- **Bookings — Organizations moved out of Settings into their own page** — clicking the **org name at the top of the sidebar** now opens a dedicated **Organizations** page (`/bookings/organizations`): a table of the orgs you manage (name · role · public booking link · Active/Switch) with **Create organization** (platform admin only). Cross-org management no longer lives inside per-org Settings; the sidebar dropdown was replaced by the clickable org name. Settings → **Team** keeps per-org member management. `auth.orgs()` now carries each org's `slug`.

### Added
- **Bookings — multi-tenant: org creation, team management, calendar isolation** — **(1) Org creation (platform-admin gated):** a new **Team** tab in Settings lets a **platform admin** create organizations (`create_org` RPC → new org + you as owner; appears in your switcher). Not self-serve — only platform admins can create. **(2) Member management:** add/remove org members by email with a role (owner/admin/staff) via `add_org_member`/`remove_org_member`/`list_org_members` RPCs (the invitee must have signed in once; can't remove the last owner). `BookingsAuthService` exposes `isPlatformAdmin` + `refresh()`. **(3) Calendar isolation (multi-tenant correctness):** the single shared Google Calendar is now gated to the org that owns it via a `CALENDAR_ORG_ID` Edge-Function secret — other orgs run **DB-only** (availability is DB-driven; the no-overlap constraint still prevents double-booking), so a second org never pollutes the owner's calendar or sees its free/busy. Applied to `ensureBookingEvent`, `start-card-booking`, `check-availability` (6 functions redeployed). **(4) Branding de-leaked:** the payment receipt + the "slot taken" page no longer hardcode John's name/site/email (the formal invoice carries proper per-org branding).
- **Bookings — multi-org foundations (towards multi-tenant)** — (1) **Multi-org admin context + switcher:** `BookingsAuthService` now resolves *all* orgs the user owns/admins (not just one) and exposes an active org with `setActiveOrg()`; a **Stripe-style org switcher** appears in the studio sidebar when you belong to more than one org (single org just shows its name). `BookingDataService` now scopes its bookings/clients queries to the **active org** (`org_id`), so switching shows only that org's data. (2) **Per-org public booking routing:** added slug-driven routes `/:org/book`, `/:org/book/calendar`, `/:org/book/checkout`, `/:org/book/mine` (the `book` 2nd segment can't collide with the map app); the public flow reads the org slug from the route and threads it through every step, and `BookingOrgService.load(slug)` re-fetches when the slug changes. The default `/book` still serves the primary org. *(Org creation + member management is the next chunk — these are the prerequisites.)*
- **Bookings — editable invoices + Invoices module** — new **Invoices** item in the studio sidebar: a ledger of every confirmed booking-as-invoice (No. · date · client · total · paid · balance), with a **year filter** and **CSV export**; each row opens the printable invoice. Invoices are now **editable**: they're generated live from the booking by default, but an admin can open **Edit invoice** (from the booking detail) and change the **line items (description + amount, add/remove), issue date, and an invoice note** — persisted to a new `invoices` table keyed 1:1 to the booking. **Editing an invoice never touches the booking, calendar or work board** (full decoupling); "Reset to booking" discards the override. Paid/balance still come from the booking's payments. The `get_invoice` RPC now returns line items (the saved override if present, else derived from the booking) so the printable invoice, client view and editor all render the same data. New `invoices` table (admin-RLS) + `BookingDataService.saveInvoice()/resetInvoice()`.
- **Bookings — printable invoices (PDF) + client billing self-service** — every booking can now produce a professional A4 **invoice** at `/book/invoice/:id`: supplier header (from the org's Company & Invoicing settings), bill-to block (the client's billing details), a line-item table (title + description, amount), a **Travel & expenses** line when set, totals, **Paid / Balance**, and **Download / Print PDF** (browser print). Invoice number = the booking ref with the prefix swapped (`BK-2026-007` → `INV-2026-007`). **VAT-aware:** VAT-registered orgs show the VAT number + net/VAT(@rate)/gross breakdown (prices treated as VAT-inclusive); non-registered shows "No VAT charged — Article 11". Admins open it from the booking detail page ("Invoice"); clients open their own from **/book/mine** ("Invoice (PDF)"). Clients can now **edit their own billing details** (name, company, VAT number, address) on /book/mine, which flow onto the invoice.
- **Bookings — SECURITY: leak-proof invoice access** — invoice data comes from a single `get_invoice(booking)` **SECURITY DEFINER** RPC that authorizes the caller as the **org admin OR that booking's own client** and returns only invoice-safe fields (no `price_revenue`, no other bookings/clients). This lets a client render their invoice — which needs the org's company details (normally behind org RLS) — without exposing admin/financial data. Anon cannot call it (must be signed in as that client). Access is enforced in the DB, not just the UI, so guessing another booking's id returns "not available".
- **Bookings — Settings reorganized into tabs + Company & Invoicing** — the Settings page is now tabbed (*General · Booking rules · Company & Invoicing · Payments · Integrations*). The new **Company & Invoicing** tab captures the org's invoicing identity — registered business name, address, phone, invoicing email, **VAT-registered toggle** (off = Article 11 small undertaking, no VAT) with VAT number + rate, an invoice-number **prefix** (default `INV`, shown as e.g. `INV-2026-007`), and an invoice footer. Stored in a new `organizations.invoice_details` JSONB (added to the admin column-grant so admins can edit it; `stripe_*` columns stay service-role-only). Foundation for the invoice generator (next). Applied live 2026-06-15.
- **Bookings — work description on every booking (client-facing)** — the New/Edit form now has a **Description** field (uses the existing `bookings.description` column) that is **auto-filled** from the service + hours (e.g. "Drone Pilot Filming — 2 hours") and editable, or **must be typed** for a Custom job — it's **mandatory** to submit. It re-suggests when you change service/hours unless you've edited it (with a "Reset to suggested" link, mirroring the price field). The client now sees it as **"Details"** at the top of the `/book/:token` pay page (so they know what they're paying for), and it already flows into the invoice. No schema change — the column existed but was never populated/shown.
- **Bookings — payment-page loading states** — clicking a pay option shows "Loading secure payment form…" until Stripe's form renders; the **Pay now** button shows a spinner + "Processing…" and disables (with Back) while the charge confirms.
- **Bookings — hide "remove payment" on Stripe charges** — the × delete control now only appears on **manual** payments (cash/Revolut/bank/other); card/Stripe charges are reversed via Cancel + refund, so the button that always errored is gone.
- **Bookings — deposit policy: org default + per-booking override** — Settings now has **"Allow deposits by default"** alongside the existing default **deposit %** (`booking_params.deposit_allowed` / `deposit_percent`). These **prefill** the New/Edit booking form, where the admin can override them **for that one booking** — e.g. require a 50% deposit on this job, or full payment only on another. Stored on new `bookings.deposit_percent` / `bookings.deposit_allowed` columns (nullable; the chosen value is saved explicitly so changing the org default later never alters an existing payment link; legacy rows fall back to the default). The public `/book/:token` page and the self-serve checkout now show the correct deposit % (or hide the deposit option entirely when full payment is required), and the payment Edge Functions enforce it server-side (`create-payment-intent`, `start-card-booking` reject a deposit when it isn't allowed). Also fixes a latent bug where the link flow hardcoded a 30% deposit regardless of the org setting.
- **Bookings — connect your own Stripe account (Stripe Connect, per-org payouts)** — Settings → **Payments (Stripe)** now has a **Connect Stripe** button that runs Stripe's hosted **Standard** Connect onboarding (sign in / create account), shows live status (*Not connected* / *Setup incomplete* / *Connected — charges enabled*), and lets you resume or manage it. Card payments are then created as **direct charges on the org's connected account** (with an optional platform application fee in `organizations.application_fee_bps`); the org is merchant-of-record for its own payouts. New Edge Functions `connect-stripe-start` / `connect-stripe-status` (both org-admin-gated, own-org-only, onboarding redirect URLs derived from a trusted `APP_BASE_URL`/Origin, never client input). Charge routing is centralized in `_shared/stripe.ts` (`resolveOrgStripe` + `chargeRouting`) and threaded through `start-card-booking`, `create-payment-intent`, and `cancel-booking` refunds; the public checkout + `/book/:token` page init Stripe.js with `{ stripeAccount }` for the direct charge. **Fallback:** an org with no connected account still charges on the platform account (so the seed org keeps working) — Connect is purely additive. New `organizations.stripe_charges_enabled` / `stripe_details_submitted` / `application_fee_bps` columns.
- **Bookings — SECURITY: payout fields locked down** — an org admin could previously `UPDATE organizations SET stripe_account_id=…` (redirecting every payout to an account they control) or flip `stripe_charges_enabled` to bypass onboarding, because `authenticated` held a table-wide UPDATE grant under the `org_update` RLS policy. Replaced with **column-level UPDATE privileges**: admins may update only `name, timezone, currency, booking_params, features`; all `stripe_*` / fee columns are writable solely by `service_role` (the Edge Functions). Defence-in-depth — even a compromised admin JWT can't change where money lands.
- **Bookings — multiple payments + payment detail page** — a booking can now hold many payments (deposit + partials + final). New **booking detail page** at `/bookings/:id` (open via the Ref link or "View details & payments" in the kebab): shows Total / Paid / Balance, a payment ledger (amount · method · note · date, with delete), and a **"Record a payment"** form — amount, method (Cash / Revolut / Bank transfer / Card / Other), optional note, date, plus a "Pay full balance" shortcut. `payments.note` column added and `method` extended to `card|cash|revolut|bank|other` (migration applied + schema updated). New `BookingDataService.getPayments/addPayment/deletePayment` (admin RLS; recording a payment refreshes the calendar description via `sync-booking-event`). The bookings table now shows **paid / total** in the Payment column. All earnings figures (Collected, Outstanding, 6-month chart, list stats) already aggregate `total_paid` across every completed payment — verified, no calc change needed. Cancel/refund prompt corrected: it only refunds **card** payments via Stripe (cash/Revolut/bank are settled manually). Deleting a card/Stripe payment is blocked in the UI.
- **Bookings — toast notifications (reusable)** — a single `ToastService` (root) + one `ToastHostComponent` mounted once in the platform shell now gives every admin action friendly feedback ("BK-2026-004 marked as paid", "approved — added to your calendar", "declined", "Booking updated", "Service created", "Moved to Editing", errors, etc.). Bottom-right on desktop, full-width bottom on mobile; auto-dismiss (errors linger), click to dismiss, reduced-motion aware. Wired across the bookings list, dashboard, new/edit form, services, staff, settings and work board — no per-page markup. High-frequency interactions (task checkboxes) intentionally stay quiet. Replaces the old `alert()` calls.
- **Bookings — per-link payment options (card / pay-later / both)** — the New/Edit booking form now has a **Payment options** choice that controls what the client sees on the `/book/:token` link: **Card or pay later** (default), **Card only**, or **Pay later only**. Stored on `bookings.allow_card` / `allow_inperson` (migration applied; both default `true`). Behaviour of the pay-later button: when **both** are offered it's a request the admin approves (calendar on approval); when **pay-later is the only option** the client's click **confirms the booking directly** (a terms-acceptance — "I agree to pay €X by cash, Revolut or bank transfer" → books + calendar immediately). The book page shows a `confirmed` vs `requested` screen accordingly; `create-payment-intent` rejects card when `allow_card` is off.
- **Bookings — pay-later option on the payment link** — the `/book/:token` page offers a **"Pay later — cash / Revolut / bank transfer"** option (new `accept-inperson` Edge Function, token-gated). When card is **also** offered, clicking it raises a **pending cash request** in the admin's **"Requests to approve"** (calendar event only on the admin's approval via `approve-cash-booking`). When pay-later is the **only** option, the click **confirms the booking** and creates the calendar event immediately (see per-link payment options above). If a client first asks pay-later but then pays by card, `stripe-webhook` confirms it (`pending`/`hold` → `booked`). (Card payment already created the event via `stripe-webhook` — unchanged.)
- **Bookings — calendar events carry a live overview (payment + work progress)** — Google Calendar event descriptions now show **Client / Service / Total / Payment (Paid in full · Deposit paid €X, balance €Y · Awaiting payment) / Progress (To edit → Editing → To deliver → Delivered)** plus notes and the ref. Centralized in `_shared/booking-event.ts` (`ensureBookingEvent` — creates the event or patches its description from current DB state) and `updateCalendarEvent` (PATCH) in `_shared/google-calendar.ts`. The description **stays in sync**: refreshed on card payment (`stripe-webhook`), cash approval (`approve-cash-booking`), in-person confirmation (`accept-inperson`), and — via the new admin-only `sync-booking-event` function called from the frontend — whenever a cash payment is recorded, the amount is edited, or a job is moved through production stages (Work board). So the owner gets the whole picture from their calendar.
- **Bookings — edit a booking + custom service/price** — every booking can now be **fully edited** (not just its amount): the `BookingFormComponent` doubles as an edit form at `/bookings/:id/edit` (prefilled from the row), updating client/service/worker/time/hours/price/title/location/notes via `BookingDataService.updateBooking()` (DB exclusion constraint still blocks moving onto an occupied worker slot → `23P01`). The form also gained a **"Custom — set your own price"** service option: no `service_id`, any bookable worker, free duration, fully manual price — for one-off jobs that aren't in the catalog (`createBooking`/`updateBooking` accept `service_id = null`; `bookings.service_id` is already nullable). New `EditableBooking` interface + `getBooking()`.
- **Bookings — manual "New booking" (rebuilt, multi-tenant)** — admin can again create a booking by hand at `/bookings/new` and get a **payment link to send the client**. Rebuilt the orphaned single-tenant form (which used the legacy `create-booking` Edge Function + hardcoded owner email) into an org/service/worker-aware form: pick or create a client → pick a **service** → its assigned **worker** → start time + hours (clamped to the service's min/max) → price **auto-filled from the service's tiered pricing** (`servicePrice()`, editable for one-offs) → optional title/location/notes. Creates a confirmed `booked` slot directly via RLS (`is_org_admin`); the DB exclusion constraint rejects worker double-bookings (`23P01` → "that worker is already booked"); ref + production tasks are set by triggers. On success it generates a `booking_links` token and shows the copyable `/book/:token` URL. New `BookingDataService.createClient()` + `createBooking()`. Route restored under the platform shell; "+ New booking" buttons on the Dashboard and Bookings list now resolve. Removed the dead `NewBookingForm` interface. Build green. (Google Calendar push still happens via the existing **Sync Calendar** button.)
- **Bookings — admin Dashboard** — new default landing at `/bookings/dashboard` (sidebar's first nav item; `/bookings` now redirects here instead of the list). At-a-glance overview built only from existing data: KPI cards (upcoming bookings in next 30 days, Collected, Outstanding, Requests to approve, In production), a "Collected · last 6 months" CSS bar chart, an **Upcoming bookings** list (next 6, with date chip + payment pill), and a **Needs your attention** panel (inline approve/decline of cash requests, outstanding-balance + Work-board shortcuts). Reuses `BookingDataService` (realtime bookings) + `BookingAdminService` (production jobs/tasks) + `booking.styles` tokens; no new queries or tables. `BookingSummary` now exposes `service_name`/`staff_name` (already returned by the `booking_summary` view). Build green.
- **Bookings — cancel + Stripe refund** — admin can **Cancel** any booking: frees the slot, removes the Google Calendar event, and (with a second confirmation) **refunds** completed card payments via Stripe (`cancel-booking` Edge Function; `deleteCalendarEvent` added to `_shared/google-calendar.ts`; `payments.status → refunded`). Two-step native confirmations show the exact amount and "cannot be undone".
- **Bookings — email/notifications spec** — `documentation/BOOKING_EMAILS.md` (provider, Edge Function, every trigger + template). Not yet built; highest-impact gap.

### Changed
- **Bookings admin — wider layout on big screens** — the Dashboard and Bookings pages now cap at 1600px (was 1100px) so they use the available width on large monitors instead of leaving a large empty right margin. Forms (Services/Staff/Settings/New booking) stay at 720px for readability.
- **Bookings — row kebab simplified around the detail page** — the old one-tap "Mark cash paid" (records the full balance) and the `prompt()`-based "Edit amount" are gone; the kebab is now **View details & payments · Record payment… · Edit booking · Cancel booking**. "Record payment…" opens the detail page's payment form (any amount/method), and the booking total is edited in the Edit form's Price field — one consistent place for money, no blocking prompts.
- **Bookings — cancel reliably removes the calendar event** — `deleteCalendarEvent` now checks the response (treats 404/410 "already gone" as success, throws on real failures), and `cancel-booking` only clears the stored `google_event_id` once the delete actually succeeds — so a transient Google error no longer orphans a ghost event with no way to clean it up. `cancelBooking` returns `calendar_cleared`; the admin list warns if the event couldn't be removed so you can delete it by hand.
- **Bookings list — row actions tidied into a kebab menu** — each row showed four text buttons (Copy link · Mark cash paid · Edit € · Cancel), which was noisy. Now there's a single primary **Copy link** button (with ✓ feedback — it's the core "send the client a payment link" action) plus a **⋯ overflow menu** (Angular CDK `@angular/cdk/menu`, overlay-positioned so it isn't clipped by the scrollable table) holding **Edit booking**, Mark cash paid (when due), Edit amount, and Cancel booking (danger-styled). CDK handles click-away + keyboard nav.
- **Bookings — revenue math corrected** — admin stats now distinguish **Expected revenue** (sum of `price_revenue` for confirmed jobs only: `booked`/`in_progress`/`done`, excluding pending/hold/cancelled/expired/external) from **Collected** (sum of `total_paid`), plus **Awaiting payment** (unpaid + partial). Previously "Total revenue" wrongly counted non-confirmed bookings.
- **Work board — drag & drop (Trello-style)** — jobs drag between stage columns via Angular CDK (`DragDropModule`), with animated preview/placeholder + drop highlighting; arrow buttons kept as a touch fallback. Moving a card persists `production_status` and realtime reconciles.
- **Bookings — DRY/structure pass** — extracted shared helpers to kill duplication: `core/utils/realtime.util.ts` (`subscribeToChanges`, used by the bookings list + Work board), `core/utils/currency.util.ts` (`currencySymbol`, used by service-picker/calendar/checkout), and `platform/admin.shared.scss` (page/btn/field/card/item primitives `@use`d by Services/Staff/Settings instead of each redefining them).
- **Bookings — multi-tenant app rebuilt (edge functions + frontend)** — the self-serve flow is now org/service/worker-aware. New/rewritten Edge Functions: `get-org-booking` (public: org + active services + bookable workers per service), `get-availability` (per worker+service — the pairing's `working_hours` minus the worker's busy ranges, returns the service's tiered pricing), `start-card-booking` + `approve-cash-booking` (org/staff/service, `service_price`, `is_org_admin`), `create-payment-intent` (adds `org_id`). Frontend: new **service→worker picker** at `/book` → `/book/calendar?service=&staff=` (range select) → `/book/checkout` (threads org/service/staff). `BookingOrgService` loads the active org (`johnfabiomb`) + services; `AvailabilityService`/`ClientPortalService`/`booking-data.service` are org-scoped; `bookings-auth` resolves admin via `org_members` (owner/admin) instead of the dropped `is_admin()`. Per-service pricing model: `services.pricing` = tiers + `extra_hour_price` (Drone 1h€100/2h€190/3h€270/4h€350 +€80/hr; Camera flat €80/hr). Verified end-to-end: a client booking Drone 2h yields €190 pending. Build green (81 routes). **Still single-tenant in practice (one org/worker); admin Services/Staff management UI + the manual "new booking" form + per-worker schedule editor are not yet rebuilt.**
- **Bookings — rebuilt MULTI-TENANT (DB foundation applied)** — consolidated all migrations + the duplicate `core/db/schema.sql` into one source of truth, `supabase/bookings-schema.sql`, and applied it to the live jm-bookings project. New model: `organizations` → `org_members` (owner/admin/staff) + `platform_admins`; `staff` (workers, one shared calendar each) → `staff_services` (M2M, per-pairing `working_hours`) → `services` (fixed duration + price); `clients`/`bookings`/`payments`/`booking_links` all carry `org_id`. No-overlap constraint now partitioned by `staff_id` (services on one worker share the calendar; different workers may overlap). Org-scoped RLS (`is_org_admin`/`current_staff_id`/`current_client_id(org)` + `is_platform_admin`) replaces global `is_admin()`. Per-org booking refs. Seeded org **johnfabiomb** with John Montaño (owner/worker/platform-admin) + two services (Drone Pilot Filming daytime, Camera Filming 24h). **The booking frontend + edge functions are NOT yet org/service-aware — they must be rebuilt before the `/book` flow works again** (see `documentation/MULTI_TENANT_ROADMAP.md`). Old `supabase/migrations/*` are superseded by the consolidated schema.

### Added
- **Bookings — Work board (post-production to-do)** — new feature-flagged admin page (`/bookings/work`, toggle in Settings → `organizations.features.work_board`). A confirmed booking (`status='booked'`) becomes a production **job** (`bookings.production_status`: to_edit → editing → to_deliver → delivered) and auto-gets a **checklist** of tasks from its service's template (`services.task_template`). New `tasks` table (org-scoped RLS) + two triggers (`set_production_status` confirms the job, `seed_production_tasks` seeds the checklist once). Kanban UI with 4 stage columns, per-job tickable tasks, add/remove task, move stage, overdue summary, **live via realtime**. Services editor gains a "Default tasks" field; nav shows "Work" only when enabled. Verified: confirmed jobs seeded (Drone 3 / Camera 4 tasks), admin reads via RLS. Build green.
- **Bookings — admin self-service setup (Services / Staff / Settings + setup guide)** — org owners can now configure everything in-app at `/bookings`: **Services** (catalog with tiered/flat pricing + min/max hours), **Staff** (add workers, assign services, set each worker's per-service hours), **Settings** (deposit %, hold/lead minutes, cash toggle, timezone, currency) + a live **Integration status** panel. New `BookingAdminService` (RLS-enforced CRUD), three admin components, and `check-integrations` Edge Function (verifies Stripe + Google credentials actually work). Admin nav updated (Bookings · Clients · Services · Staff · Settings); the old single-tenant `/bookings/new` + `/bookings/hours` routes removed. New **`documentation/BOOKING_SETUP.md`** documents the external steps the owner must do once: Stripe (keys → Supabase secrets, webhook endpoint + signing secret), Google Calendar (Cloud project, Calendar API, publish OAuth consent, OAuth client, refresh token via OAuth Playground, secrets), Supabase Auth redirect URLs. Verified: admin CRUD via RLS works as the org owner. Build green (81 routes). Deferred: manual admin "new booking" form, per-worker Google Calendar, per-org Stripe Connect (multi-org).
- **Self-serve booking — Phase 4 (client portal + booking_summary lockdown)** — `/book/mine`: signed-in clients see their own bookings (status, date, amount, balance) and can **view an invoice** (reuses `booking-invoice`). `ClientPortalService.loadMyBookings()` reads base tables scoped to the client's own `client_id` (client-safe fields only — no expenses/revenue). Checkout success now links to it. **Security fix:** `booking_summary` was SECURITY DEFINER and granted to all authenticated users, so a signed-in client could read every booking incl. client contact + internal `price_expenses`/`price_revenue` (and the broad pay-link RLS policy widened it further). Recreated the view gated by `WHERE is_admin()` (migration `2026-06-12_phase4_client_portal.sql`); verified admin sees all, non-admin sees zero. Builds green.
- **Self-serve booking — Phase 3 (admin approvals + reconciliation)** — the admin bookings list now shows a **"Requests to approve"** section for `pending` cash requests with **Approve** / **Decline**, and per-booking **Mark cash paid** + **Edit €** actions. `approve-cash-booking` Edge Function (admin-only, verifies `user_roles.role='admin'`): flips `pending → booked` (DB constraint → `slot_taken` if the slot was taken meanwhile) and pushes the event to Google Calendar. `BookingDataService` gains `approveRequest`/`declineRequest`/`recordCashPayment`/`setAmount`; `BookingStatus` type extended with `pending`/`hold`/`expired`. Verified: cash payment recording flips `payment_status → paid` in `booking_summary`. Builds green; function deployed.
- **Self-serve booking — Phase 2b (card payments)** — checkout now offers **Pay in full** / **Pay 30% deposit** (Stripe Elements inline) alongside the cash request. New `start-card-booking` Edge Function (JWT-protected): lazily expires stale holds, runs a final live Google FreeBusy check, creates a 15-min `hold` (DB exclusion constraint → returns `slot_taken` on conflict), and creates a Stripe PaymentIntent (deposit/full). `stripe-webhook` updated to flip `hold → booked` + clear `hold_expires_at` on payment, then push to Google Calendar. `get_busy_ranges()` now ignores expired holds (abandoned checkouts free the slot); `booking_params.deposit_percent` set to 30 to match the existing book-page flow. Migration `2026-06-11_phase2b_card.sql`. Builds green; functions deployed. NOTE: uses **live** Stripe keys — test charges are real money (or switch to test keys first). — `/book/checkout?start=&hours=`: re-confirms the slot is free, gates on sign-in (Google + magic link via `ClientPortalService`, redirecting back to checkout), collects billing (name/email/company/VAT/address), and submits a **cash request**. Two server RPCs (`supabase/migrations/2026-06-11_phase2a_client_booking.sql`, applied): `upsert_my_client` (links/claims the client row for the auth user — claims an admin-created row by email) and `create_booking_request` (creates a `pending` booking with a **server-set** price from `admin_settings.pricing`, so the client can't tamper). Calendar "Continue" now navigates here. Card path stubbed for Phase 2b. Verified: client RPC chain yields `pending` / €190 / `created_by=client`; prod build green. NOTE: add `/book/checkout` (and origin) to the jm-bookings Supabase **Auth → URL Configuration** redirect allowlist for OAuth to return correctly.
- **Self-serve booking — Phase 1 (public availability calendar)** — read-only Calendly-style page at `/book`: duration picker (1–4h with prices), month calendar highlighting days with free slots, time-slot list (shown in Europe/Malta local time), and a sticky summary with the price. Backed by `AvailabilityService` → `get-availability` Edge Function (deployed, `--no-verify-jwt`, public): given `{from, to, durationHours}` returns free start-times per day = working hours minus `get_busy_ranges()`, DST-correct wall-clock→UTC, `min_lead_minutes`/`buffer_minutes` applied, plus pricing. Detail-free (no titles/clients). No login/booking yet — "Continue" is stubbed for Phase 2. Verified: 2h slots return local 07:00–18:00; prod build green.
- **Self-serve booking — Phase 0 (DB foundation)** — `supabase/migrations/2026-06-11_phase0_self_serve_booking.sql`, applied to live jm-bookings DB. Adds: `clients.user_id` (link auth user ↔ client) + `vat_number` + `billing_address`; `bookings.hold_expires_at` + `created_by`; `payments.method` (card/cash); booking statuses `pending` (cash request, doesn't block slot), `hold` (card checkout, 15-min TTL), `expired`; `admin_settings` keys `pricing` (fixed price per 1–4h: €100/€190/€270/€350) and `booking_params` (hold_minutes, min_lead_minutes, deposit_percent, cash_allowed). **Concurrency guarantee:** `btree_gist` exclusion constraint `bookings_no_overlap` makes overlapping held/confirmed bookings physically impossible (fails with `23P01`). Client RLS (see/manage only own; can only insert `pending`/`hold` for self) + `current_client_id()` and detail-free `get_busy_ranges()` (public calendar availability) functions. Verified: overlap rejected, pending request allowed to overlap.

### Changed
- **Checkout billing — only name + email required** — Company, VAT/tax number, and billing address are now optional (labelled as such); `canSubmit()` only requires name + email. The `upsert_my_client` RPC already stored blanks as NULL.
- **Self-serve calendar — select a time range, not a preset duration** — replaced the up-front 1–4h duration picker with admin-picker-style range selection: pick a day → tap a start hour → tap an end hour; the span sets the duration (capped at `maxHours` from pricing) and price. `get-availability` now returns each day's hourly slots (`{start, hour, label, available}`) instead of duration-filtered starts; calendar enforces contiguous + all-free + ≤ maxHours; checkout re-validates the full span. Redeployed + verified.
- **Booking module reorganized by feature** — replaced the type-based layout (`core/` junk drawer, `features/admin/`, `pages/`) with a feature-based structure: `core/{db,services,interfaces,guards}/`, `auth/login/`, `platform/{platform-shell,bookings,clients,my-calendar}/`, `public/{book-page,payment-success}/`, `ui/`. Guards extracted from `booking.routes.ts` → `core/guards/booking-auth.guards.ts`; working-hours types extracted from the service → `core/interfaces/working-hours.interface.ts`. Renames: `admin-shell`→`platform-shell`, `bookings-list`→`booking-list`, `new-booking`→`booking-form`, `clients`→`client-list`, `working-hours-page`→`my-calendar`. Updated `@booking/*` path aliases (tsconfig) and the `supabase.bookings.ts` fileReplacement path (angular.json). Pure restructure — no behavior change; prod build verified (81 routes prerendered)

### Fixed
- **Bookings auth — `onAuthStateChange` deadlock (dashboard stuck loading, "Supabase project may be paused")** — the `onAuthStateChange` callback in `bookings-auth.service.ts` `await`ed `resolveRole()` (a `user_roles` query) directly. Supabase awaits this callback *inside* its own `initializePromise`, while every query awaits `getSession()` → `initializePromise` → circular deadlock: `getSession()` never resolved, so `booking_summary`/`clients`/`user_roles` queries all hung and hit the 15 s timeout × 3. Fixed by deferring the role lookup with `setTimeout(0)` so init resolves first. The live DB was healthy throughout — grants/RLS/`is_admin()` verified correct via the Management API
- **Bookings auth — `resolveRole()` error handling** — `resolveRole()` was calling `state.set('no-access')` when the `user_roles` query returned a Supabase error object; Supabase returns `{ data: null, error }` without throwing so the `catch` block never fired. Added explicit `if (error)` guard — transient DB errors no longer downgrade a cached admin session
- **Bookings data loading — 15 s timeout** — increased per-attempt query timeout from 8 s to 15 s to allow for PostgREST cold-start latency
- **`bookings-schema.sql` — `service_role` grants missing** — SQL-created tables do not get automatic Supabase grants; Edge Functions use `SUPABASE_SERVICE_ROLE_KEY` but all queries returned "permission denied" because no `GRANT ... TO service_role` statements existed. Added `GRANT ALL` for `service_role` on all booking tables; applied to live DB via Management API
- **`bookings-schema.sql` — `is_admin_user()` wrong table** — function referenced `public.users` which does not exist in `jm-bookings`; live DB had the correct `is_admin()` function querying `user_roles`. Schema corrected to match; all policy references updated from `is_admin_user()` to `is_admin()`
- **`bookings-schema.sql` — `public read linked booking` RLS self-reference** — EXISTS subquery compared `booking_links.booking_id = booking_links.id` (same-table columns) instead of `bl.booking_id = bookings.id` (outer row). Fixed in schema and applied to live DB
- **`bookings-schema.sql` — `user_roles` and `admin_settings` tables not defined** — both tables used by app code but never in the schema; added `CREATE TABLE IF NOT EXISTS` for both
- **Booking schema — anon RLS** — `/book/:token` page can now load booking data without auth; added `GRANT SELECT ON booking_links TO anon`, `GRANT SELECT ON bookings TO anon`, and a `"public read linked booking"` policy on the `bookings` table (previously anonymous users got null because the table-level grant was missing)
- **Booking schema — idempotent policies** — all `CREATE POLICY` statements now prefixed with `DROP POLICY IF EXISTS` so the schema can be re-run safely
- **`is_admin()` function** — schema now consistently uses `user_roles` table (previously an older draft of `bookings-schema.sql` referenced `public.users` which doesn't exist in the jm-bookings project)
- **~~`supabase/config.toml`~~ — this entry was wrong; the file never landed.** No `config.toml` exists in the repo and none was ever tracked. Function deploys target the project explicitly: `supabase functions deploy <fn> --project-ref odmwjhysvvbhxytyefhv`. Consequence worth knowing: there is no per-function `verify_jwt` block, so any function that must accept unauthenticated callers — notably **`stripe-webhook`** — has to be redeployed with an explicit `--no-verify-jwt`, or every Stripe callback 401s.
- **NavigatorLockAcquireTimeoutError** — Supabase Web Locks API conflict on HMR/hot-reload; fixed by providing a custom no-op `lock` bypass in both `supabase.bookings.ts` and `supabase.bookings.production.ts`
- **Map Supabase 403 bad_jwt on `/bookings`** — map routes are now lazy-loaded (`loadChildren`) in `app.routes.ts`; the map Supabase client and `AuthService` are no longer initialised when navigating to booking pages
- **Sync Calendar hanging** — `syncCalendar()` in `BookingDataService` now races against a 10 s timeout so the UI doesn't freeze when the Edge Function isn't deployed yet

### Changed
- **Booking module restructured by route** — `bookings-shell` (monolithic auth-gate + tab switcher + all views in one component) replaced with a clean route-based architecture:
  - `/bookings/login` — dedicated login page with Google + magic link sign-in; auto-redirects to `/bookings/list` if already authenticated
  - `/bookings` → redirects to `/bookings/list` via Angular router
  - `/bookings/list` — `BookingsListComponent`: stats, bookings table, copy-link button
  - `/bookings/new` — `NewBookingComponent`: two-column form (client, job details, slot picker, pricing); success state with generated link
  - `/bookings/clients` — `ClientsComponent`: clients table
  - `/bookings/hours` — `WorkingHoursPageComponent`: thin wrapper around `WorkingHoursEditorComponent`
  - `AdminShellComponent` — layout shell with dark sidebar (`RouterLink`/`RouterLinkActive` nav), Sync Calendar button, `<router-outlet>`; provides scoped `BookingDataService` (not root)
  - `BookingDataService` — extracted data + sync logic from `bookings-shell` into a dedicated service provided at `AdminShellComponent` level; all child views share the same data instance
  - `loginGuard` + `adminGuard` — route guards using `BookingsAuthService.initialize()` (idempotent) for clean redirect logic on every navigation
  - `booking.routes.ts` — updated with nested route structure under `bookings/`, old flat route removed
  - `bookings-shell/` directory deleted
- **Two-module architecture** — `src/app/` now contains two fully isolated modules: `booking/` and `map/`, each with identical `core/`, `features/`, `pages/`, `ui/` sub-structure; either can be extracted as a standalone Angular library without touching the other
- **Path aliases renamed for clarity** — `@core/*`, `@features/*`, `@ui/*`, `@layout/*`, `@pages/*` renamed to `@map/core/*`, `@map/features/*`, `@map/ui/*`, `@map/layout/*`, `@map/pages/*`; booking module now uses `@booking/core/*`, `@booking/features/*`, `@booking/ui/*`, `@booking/pages/*`
- **`feature-flags.ts` moved to `src/app/map/`** — it is map-only; `angular.json` fileReplacements updated for both production and staging configs
- **Stale `angular.json` path fixed** — production build now correctly replaces `src/app/map/core/config/supabase.config.ts` (was pointing at a deleted `src/app/shared/config/` path)
- **Booking Supabase config** — added `src/environments/supabase.bookings.production.ts`; production build swaps it in via `angular.json` fileReplacements, matching the map Supabase pattern
- **Route files extracted by module** — `src/app/booking/booking.routes.ts` owns all booking routes; `src/app/map/map.routes.ts` owns all map routes; `app.routes.ts` is now 7 lines that combine both
- **`MapRootComponent`** — moved all map-specific overlays (auth modal, welcome popup, level modals, PWA prompt) out of `app.component` into `src/app/map/map-root.component.ts`; `app.component.ts` is now a 10-line shell with a single `<router-outlet>`

### Added
- **Booking & payment system** — full end-to-end replacement for manual Google Sheets + Stripe Buy Button workflow:
  - `supabase/bookings-schema.sql` — `clients`, `bookings`, `payments`, `booking_links` tables; `booking_summary` view; RLS; `booking_ref` sequence (BK-2026-001 format)
  - 5 Supabase Edge Functions: `create-booking`, `check-availability`, `create-payment-intent`, `stripe-webhook`, `sync-calendar`
  - `/book/:token` — public payment page; shows job details, checks Google Calendar availability, offers 30% deposit or full payment (past-date = full only); Stripe Elements
  - `/bookings` — admin-only dashboard (tab: Bookings table, New booking form, Clients list); create bookings, generate shareable payment links, copy to clipboard
  - Google Calendar integration via OAuth2 refresh token — auto-creates event on payment, availability check before payment
  - Stripe webhook (`payment_intent.succeeded`) marks payment completed and triggers GCal event creation

### Added
- **Edit profile modal** — users can update their display name and phone number from the profile popup (Edit profile button above Sign out); opens a centered modal with backdrop
- **First-login name prompt** — magic link users with no display name are shown a blocking modal on first login asking for their name before they can proceed; name is written to both `users.display_name` and `auth.user_metadata` so it propagates everywhere
- `users.phone` column added to Supabase schema (run `ALTER TABLE public.users ADD COLUMN IF NOT EXISTS phone TEXT NULL` in SQL Editor)
- `EditProfileModalService` — UI-state-only service (open/close/mode) following the same pattern as `LevelsModalService`
- `UserDataService.updateProfile()` — single method that writes display name + phone to `users` table and patches `auth.user_metadata.full_name`
- `UserDataService.needsDisplayName` computed signal — true when logged in and `user_metadata.full_name` is absent
- `UserDataService.phone` signal — loaded from DB on login, kept in sync after profile update

### Fixed
- **SEO / canonical URLs** — all canonical tags and sitemap `<loc>` entries now use trailing slashes to match GitHub Pages' server-level `/path` → `/path/` redirect; eliminates "Page with redirect" in Google Search Console for all prerendered routes
- **SEO service refactor** — extracted all page/default/trend config data into `seo.config.ts`; `seo.service.ts` is now pure logic with a single private `apply()` method; eliminates ~200 lines of duplicated `updateTag()` calls
- **Panel shell** — scrolling up through panel content no longer hijacks into a panel drag once `scrollTop` reaches 0; drag is only intercepted if the touch started with the content already at the top
- **Group members modal** — confirm popup now renders at `document.body` level when `fixed=true`, escaping the modal's stacking context so it overlays correctly instead of appearing trapped inside the card
- **Group members modal** — bulk-select checkboxes now guard by `group.leaderId` instead of `m.role === 'leader'` so the leader row is never selectable regardless of the role field value; `bulkRemoveMembers` also filters out the current user's UID as a safety net
- **Group chat** — group leader (admin) now always sees the chat input; previously `isMember` was `false` for the leader so they saw "Join the group to chat" instead of the message box

### Changed
- **Benefits list** — "Level up and get benefits" added as the first benefit item in `BenefitsListComponent`
- **Benefits list** — extracted into a shared `BenefitsListComponent` (`src/app/ui/benefits-list/`); both `auth-modal` and `welcome-popup` now render the same 6 up-to-date benefits; removed duplicate inline markup and styles from both components

### Added
- **Paid group join contact form** — joining a paid group intercepts the join button and shows an inline form asking for a phone number; the guide sees each member's phone in the Members modal; free groups join instantly as before; `contact_phone` stored on `group_members`
- **Guide role** — new `UserRole.Guide` (`'guide'`) in `users.role` constraint; `UserDataService.isGuide()` and `canSetPrice()` computed signals; admin panel "Grant guide role" section (email-based, same pattern as Groups activation)
- **Paid group tours** — `price_eur DECIMAL(8,2)` column on `groups` table; guides and admins can set a price when creating or editing a group; price field visible but disabled for regular users with a lock hint; `€X per person` badge on group detail, `€X` / `Free` pill on group cards
- `activate_guide_role(p_email TEXT)` Supabase RPC — sets `users.role = 'guide'` for the given email; admin-only via `GroupsService.activateGuideRole()`
- **XP & Level system** — 7-level progression (New → Explorer → Legend) driven by XP earned from location views, saves, group actions, daily login, session time, and friend referrals; level gates group creation; each level raises max-member cap
- `award_xp` Supabase RPC — atomic XP + level update with daily caps, unique-index deduplication, and level-up detection
- `InteractionTrackingService` — per-user stats counter via `track_interaction` RPC; tracks view/save/share counts per location and view/book_now/coupon_copy/share counts per provider; signals-based `stats` map for reactive reads
- `user_interaction_stats` table — composite PK `(user_id, entity_type, entity_id)`; unified table covering both locations and providers
- `xp_events` table — audit log of every XP-earning event with action, ref_id, and timestamp
- `user_profiles` view — postgres-owned view exposing safe user fields (id, display_name, photo_url, level, role) for cross-user JOINs without exposing PII; bypasses users RLS
- "You've visited this N times" chip on location pages (shown after second visit)
- Levels modal accessible globally via `LevelsModalService`; auto-opens on first login
- Level-up toast — fixed bottom pill that appears when user crosses a level threshold
- Referral system — unique 8-char referral code per user; share link in account popup; `process_referral` RPC awards 120 XP to referrer and 30 XP welcome bonus to new user
- Session heartbeat — awards 5 XP every 10 min while active (max 30 XP/day)
- Daily login bonus — 80 XP once per calendar day
- `ShareButtonComponent` — new `(shared)` output event for tracking; fires on both native share and clipboard copy
- `ProviderDetailComponent` — new `(couponCopied)` and `(providerShared)` outputs for provider interaction tracking

### Changed
- Groups now open to all users by default (`feature_access.groups = true` default); opt-out locking still available per user
- Groups list sorted by leader level descending (admin/GM first, then level 6→1), then by date
- `join_group` RPC now resolves caller profile server-side — no client-supplied name/photo (prevents spoofing)
- Groups architecture migrated from denormalized leader/member columns to live JOINs via `user_profiles` view

### Added
- **Explore Together** — full hiking groups feature behind `FEATURES.GROUPS` flag: create groups for Malta spots, join/leave with race-safe transactions, real-time member list, group chat (last 100 messages), leader transfer, cancel group
- `GroupsService` — all Firestore operations: `onSnapshot` listeners, `runTransaction` for join, `writeBatch` for atomic multi-doc writes, `fetchGroupsForSpot` one-shot query for location detail widget
- `GroupCardComponent` — group card with leader avatar, date, difficulty badge, member avatars
- `MemberAvatarsComponent` — overlapping avatar bubbles with `+N` overflow
- `ExploreTogetherComponent` — groups list + inline create form at `/malta/groups`
- `GroupDetailComponent` — group detail, member list with last-active, chat, leadership controls at `/malta/groups/:id`
- Footer **Explore nav menu** — single trigger button replaces "Browse All" + "Groups" buttons; popup with Browse Locations, Exclusive Deals, Explore Together items; backdrop overlay on open
- "Who's exploring here?" section in location detail when groups exist for that spot

### Fixed
- Footer profile popup clipped by `overflow: hidden` on `.footer-right` — removed, text truncation handled by inner elements
- Footer nav popup positioning broken by `backdrop-filter` containing block — moved popup to direct child of `.bottom-bar`
- Navigation lock icon removed from Google Maps direction buttons (was purely cosmetic, never gated)
- Explore Together and Group Detail panels had no side padding — wrapped content in `.et-content` / `.gd-content` div

### Added (directions gate + sign-in nudge)
- Guests clicking a Google Maps direction now get a 10-second interstitial countdown instead of a hard login block; a "Sign in to open straight away" nudge with a sign-in button appears in the interstitial slot
- Logged-in users skip the interstitial entirely — Google Maps opens immediately in a new tab
- Direction buttons updated: lock icon removed, hint now reads "Opens in 10s · sign in to skip" for guests
- Two new benefits added to both auth modal and welcome popup: ⚡ "Open Google Maps instantly — no wait" and 📶 "Browse the map offline, even without signal"

### Changed (copy & tone)
- Replaced all instances of "exclusive" in UI copy with honest, peer-to-peer language ("Local Deals", "Partner deal", "real discounts from local partners I trust")
- Auth modal title: "Sign in to unlock it all" → "It's way better signed in"
- Auth modal benefits aligned with welcome popup wording
- Email sent state: "Check your email" → "Link on its way! ✉️"; "We sent..." → "I've sent..."
- Saved places empty state: more personal, first-person copy
- Deals intro: "Exclusive discounts" → first-person "partners I've personally connected with"
- iOS IAB note: rewritten to friendly peer-to-peer tone
- Welcome popup subtitle updated to first-person; benefit copy aligned across both modals

### Added (auth UX overhaul)
- `SignInFormComponent` — shared reusable form used by both `AuthModalComponent` and `WelcomePopupComponent`; handles all four states: `default`, `android-redirect`, `email-input`, `email-sent`; emits `stateChange` so parents can show/hide chrome conditionally; `:host { display: contents }` slots into any parent flex layout
- `InAppBrowserService.isAndroid()` — added to gate Android Chrome redirect vs iOS email-only flow
- `AuthService.openLoginModal()` — now no-ops if user is already logged in (prevents ghost modal)
- Footer sign-out: `signingOut` loading state disables button and shows "Signing out…" while Firebase `signOut()` resolves
- `effect()` in both `AuthModalComponent` and `WelcomePopupComponent` auto-dismisses when `isLoggedIn()` becomes true, regardless of which sign-in path was used
- `busy` getter guards all sign-in buttons against double-click race conditions

### Changed (auth UX overhaul)
- `AuthModalComponent` rewritten as a thin wrapper around `SignInFormComponent`; header and benefits shown only while `formState === 'default'`; legal text hidden on `email-sent` state
- `WelcomePopupComponent` rewritten to embed `SignInFormComponent`; welcome chrome (title, benefits) collapses once form advances past `default` state
- Duplicate sign-in logic (Google OAuth, email link, error handling, loading flags) removed from both modal/popup and centralised in `SignInFormComponent`

### Added (IAB auth)
- `InAppBrowserService` — detects Instagram/Facebook/Line in-app browsers; `openInChrome()` redirects Android IAB users to Chrome via `intent://` scheme
- `AuthModalComponent` — IAB-aware states: Android auto-redirects to Chrome; iOS shows email magic link flow (`email-input` → `email-sent` with numbered steps + manual "Check sign-in" button)
- `AuthService.sendEmailSignInLink()` — sends Firebase email sign-in link; email embedded in `continueUrl` for cross-browser sign-in completion
- `AuthService.completeEmailSignIn()` — called on app init to detect and complete a pending email link sign-in from any browser; cleans URL after completion
- `AppComponent.handleEmailSignInLink()` — checks `window.location.href` on startup and completes email link auth if detected, then navigates to `/malta`

### Added
- `WelcomePopupComponent` — one-time guest welcome modal (shown 2.5s after first visit, suppressed once dismissed via `vm_welcome_shown` localStorage key); lists save/deals/directions benefits; "Sign in with Google" CTA + "Continue as guest" dismiss

### Changed (auth modal)
- Redesigned `AuthModalComponent` to match welcome popup style: same emoji + photo header, benefit rows, branded gold "Continue with Google" button, matching bounce animation

### Changed
- Dev about button: photo reduced to 65% opacity (85% on hover) + gold "i" info badge overlaid at bottom-right so users know it's tappable
- Save buttons (header + body): pulsing `deals-pulse` glow animation while unsaved; animation stops once saved

### Changed (prior)
- Design token audit: replaced all hardcoded hex colours and pixel values with `var(--token)` references across all component SCSS and inline `styles:` arrays — affects `app.component.scss`, `footer.component.scss`, `map.component.scss`, `nav-interstitial.component.scss`, `location-detail.component.scss`, `provider-card.component.scss`, `provider-detail.component.scss`, `image-gallery.component.scss`, `about.component.scss`, `contact.component.scss`, `map-shell.component.scss`, `payment.component.scss`, `payment-success.component.scss`, `route-builder.component.scss`, `saved-places.component.scss`, `top-places.component.scss`, and inline styles in `auth-modal`, `confirm-popup`, `coupon-reminder`, `pwa-prompt`, `share-button`, and `provider-avatar` components

### Added
- Firebase Authentication with Google OAuth — sign-in modal, auth state persisted via `onAuthStateChanged`
- `AuthService` — signals-based service (`user`, `isLoggedIn`, `showLoginModal`); `signInWithGoogle()`, `signOut()`, `openLoginModal()`
- `UserDataService` — eagerly instantiated at app startup; creates Firestore user document on first login with `role: 'explorer'` and `savedLocations: []`; supports `toggleSaveLocation()`
- `AuthModalComponent` — overlay modal with Google sign-in button, backdrop dismiss, loading state
- Footer: "Sign in" button shown when logged out; logged-in user's Google avatar shown when signed in
- Footer: user popup (above avatar) shows name, email, and "Sign out" button
- Provider detail: coupon code blurred behind login; phone number locked behind login; Book Now locked behind login
- Block Hotel provider data corrected: name, tagline, description, highlights, discount label and instructions

### Fixed
- `UserDataService` was never instantiated at startup — `AppComponent` now eagerly injects it so the `effect()` watching auth state fires on login

---

## 2026-05-20

### Fixed
- Provider pins now correctly hidden in production behind `FEATURES.PROMOTIONS` flag — guarded both the `providerPins` setter and the `setupProviderLayer()` init path
- Google Maps interstitial was showing 3s countdown instead of 6s — `navDuration` was being overridden to 3 in deals and malta-map components
- Footer "John Montano" about popup rendered under the mobile panel (z-index 200 < panel 300) — raised footer bar to z-index 400
- Provider card in interstitial modal overflowed the card boundaries — wrapped `ng-content` slots in `.slot-wrap` containers
- Ghost click on mobile: panel opening so fast after a location tap that the 300ms synthetic click landed on panel elements — CSS `pointer-events: none` + 350ms animation delay on touch devices

### Added
- `ProviderAvatarComponent` — reusable circular avatar with emoji badge overlay, size-aware scaling
- `CouponReminderComponent` — shows provider discount label and copyable coupon code in the Book Now interstitial
- `provider.utils.ts` — `resolveProviderColor()`, `getProviderAccentColor()`, `getProviderCategoryLabel()` utilities
- Per-provider `color` field on Provider model for brand color overrides
- Smart interstitial provider selection: primary = providers near selected location (by `nearLocationIds` or ≤8km), fallback = nearest to user GPS labeled "Near you"
- Deals page providers sorted by distance from user GPS (fetched once on init)
- Book Now interstitial: no countdown timer (duration=0), button immediately active
- Both interstitial modes (Book Now + Google Maps) open in a new tab
- Santa Maria Water Sports brand color (`#0d3d72`) and gallery images

### Changed
- Nav interstitial refactored into a generic shell with `ng-content` slots (`[slot-top]`, `[slot-bottom]`) — decoupled from provider logic
- Provider card stripe, deal strip, exclusive badge, avatar border, and map pin border all use per-provider color via `resolveProviderColor()`
- Gozo map cluster split into 7 sub-locality clusters using `Gozo-` prefix (Gozo-Dwejra, Gozo-Gharb, Gozo-Xagħra, Gozo-Munxar, Gozo-Victoria, Gozo-Marsalforn, Gozo-Qala)
- `map-rotated` host class update wrapped in `NgZone.run()` so the blue compass indicator appears immediately on mobile pinch-rotate

---

## 2026-05 (earlier)

### Added
- Persistent map shell (`MaltaShellComponent`) — single `<app-map>` instance shared across `/malta`, `/malta/list`, `/malta/deals` via router-outlet
- `MapBridgeService` — scoped communication bridge between shell and panel child components
- Instagram-style bottom sheet panel with velocity snap and pull-to-collapse on mobile
- Device-orientation compass on route map (iOS permission from user gesture, `deviceorientationabsolute` for Android)
- Location list page (`/malta/list`) with OnPush + Signals to fix iOS Safari freeze
- Exclusive Deals page (`/malta/deals`) with provider cards and map pins
- `PanelResize` utility — drag-to-resize and minimize/expand logic shared across all map panels
- Image optimisation pipeline — all images converted to WebP with map pin thumbnails

### Fixed
- Filter bar flashing on hard reload at a location URL
- iOS scroll broken after panel height chain fix
- Panel header not sticky on scroll

### Changed
- Project renamed from FPV Pilot Page to Venture Map
- Routing moved to hash-free approach

---

## 2025 and earlier

### Added
- OpenLayers interactive map with location pins, locality clusters, and filter chips
- 73 Malta & Gozo locations with descriptions, images, difficulty ratings, and GPS routes
- SEO: prerendering, sitemap, structured data, per-location meta tags
- Google Analytics integration
- Payment section (`/pay`)
- 30 Places 2026 editorial page (`/malta/30-places-2026`)
- PWA install prompt
- Privacy, cookies, and contact pages
- Google Ads infrastructure
